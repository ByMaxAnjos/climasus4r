% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_sensitivity.R
\name{sus_mod_sensitivity}
\alias{sus_mod_sensitivity}
\title{Stratified Climate-Health Sensitivity Analysis from DLNM Fits}
\usage{
sus_mod_sensitivity(
  fits,
  hot_percentile = 0.99,
  cold_percentile = 0.01,
  stratum_labels = NULL,
  alpha = 0.05,
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{fits}{A named list of \code{climasus_dlnm} objects, one per stratum.
All fits must use the same \code{climate_col} and were produced by
\code{sus_mod_dlnm()}. If the list is unnamed, strata are labelled
\code{"Stratum 1"}, \code{"Stratum 2"}, etc.}

\item{hot_percentile}{Numeric in (0, 1). Quantile of the observed exposure
distribution used as the "hot" comparison point. Default: \code{0.99} (99th
percentile — extreme heat). Must be greater than \code{cold_percentile}.}

\item{cold_percentile}{Numeric in (0, 1). Quantile of the observed exposure
distribution used as the "cold" comparison point. Default: \code{0.01} (1st
percentile — extreme cold). Must be less than \code{hot_percentile}.}

\item{stratum_labels}{Named character vector or \code{NULL}. Custom display labels
for the strata, with names matching \code{names(fits)}. If \code{NULL}, the list
names are used as-is. Useful for plotting (e.g.,
\code{c(age_65plus = "65+ years", age_1864 = "18-64 years")}).}

\item{alpha}{Numeric. Significance level for confidence intervals.
Default: \code{0.05}.}

\item{lang}{Character. Language for messages: \code{"pt"} (default), \code{"en"},
\code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{climasus_sensitivity} list with:
\describe{
\item{\verb{$rr_table}}{Tibble with one row per (stratum, component):
\code{stratum}, \code{label}, \code{component} (\code{"hot"} or \code{"cold"}),
\code{quantile_prob}, \code{exposure}, \code{rr}, \code{rr_lo}, \code{rr_hi}, \code{ref_exposure}.}
\item{\verb{$comparison}}{Tibble ranked by \code{sensitivity_index} (descending):
one row per stratum with \code{hot_rr}, \code{hot_rr_lo}, \code{hot_rr_hi},
\code{cold_rr}, \code{cold_rr_lo}, \code{cold_rr_hi}, \code{sensitivity_index},
\code{hot_rank}, \code{cold_rank}.}
\item{\verb{$stratum_curves}}{Tibble with the full exposure-response curve
for each stratum (100-point grid from each \verb{$pred}): columns
\code{stratum}, \code{label}, \code{exposure}, \code{rr}, \code{rr_lo}, \code{rr_hi}. Suitable
for overlaid ERC plots.}
\item{\verb{$meta}}{List: \code{climate_col}, \code{n_strata}, \code{stratum_names},
\code{hot_percentile}, \code{cold_percentile}, \code{alpha}, \code{call_time}.}
}
}
\description{
Compares exposure-response curves and cumulative relative risks across
population strata (e.g., age groups, sexes, income quintiles, municipalities)
to identify which subgroups are most sensitive to a climate exposure.
Takes a named list of \code{climasus_dlnm} fits — one per stratum — and extracts
the cumulative RR at user-specified hot and cold percentiles for each,
producing a ranked comparison table and full stratum-specific ERCs for
plotting.
}
\section{Statistical framework}{


For each stratum \eqn{s} with fitted DLNM \eqn{\hat{f}_s}, the cumulative
RR at exposure quantile \eqn{q} (relative to the stratum's reference
\eqn{x_{0,s}}) is:

\deqn{RR_s(q) = \exp\!\left\{\sum_{l=0}^{L}
  \left[\hat{f}_s\!\left(F_s^{-1}(q),\, l\right)
        - \hat{f}_s\!\left(x_{0,s},\, l\right)\right]\right\}}

where \eqn{F_s^{-1}(q)} is the \eqn{q}-th quantile of the observed
exposure distribution in stratum \eqn{s}. RRs are extracted by linear
interpolation from the pre-computed \code{dlnm::crosspred} grid stored in
each \code{climasus_dlnm} object — no re-fitting or additional \code{dlnm} calls
are needed.

The \strong{sensitivity index} for stratum \eqn{s} is defined as:

\deqn{SI_s = \log RR_s(\texttt{hot\_percentile}) + \log RR_s(\texttt{cold\_percentile})}

a combined measure of total climate-attributable vulnerability (heat + cold).
}

\examples{
\dontrun{
# Fit separate DLNMs per age group
fits <- list(
  elderly = sus_mod_dlnm(df_65plus, ...),
  adults  = sus_mod_dlnm(df_1864,   ...),
  youth   = sus_mod_dlnm(df_under18, ...)
)

sens <- sus_mod_sensitivity(fits, lang = "pt")
print(sens)          # ranked comparison table
tidy(sens)           # one-row-per-stratum tibble
}

}
\references{
Baccini, M., et al. (2011). Heat effects on mortality in 15 European cities.
\emph{Epidemiology}, 22(1), 68-77. \doi{10.1097/EDE.0b013e3181fdcd5e}

Benmarhnia, T., et al. (2015). A difference-in-differences approach to
assess the effect of a heat action plan on heat-related mortality, and
differences in effectiveness according to sex, age, and socioeconomic status.
\emph{Environmental Health Perspectives}, 124(11), 1694-1699.
\doi{10.1289/ehp.1510173}

Armstrong, B.G., et al. (2017). The role of humidity in associations of
high temperature with mortality. \emph{Epidemiology}, 28(6), 781-789.
}
\seealso{
\code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}, \code{\link[=sus_mod_af]{sus_mod_af()}}, \code{\link[=sus_mod_pool]{sus_mod_pool()}}
}
