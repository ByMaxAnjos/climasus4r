% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_grid_smvi.R
\name{sus_grid_smvi}
\alias{sus_grid_smvi}
\title{Import Soil Moisture Volatility Index (SMVI / Flash Drought) Data}
\usage{
sus_grid_smvi(
  years = NULL,
  municipalities = NULL,
  aggregate_by = c("year", "month"),
  brazil_only = TRUE,
  use_cache = TRUE,
  cache_dir = "~/.climasus4r_cache/smvi",
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{years}{Integer vector. Years to include. Must be within 1990–2021.
\code{NULL} (default) returns all available years.}

\item{municipalities}{An \code{sf} POLYGON object (e.g., from
\code{\link[geobr:read_municipality]{geobr::read_municipality()}}). When provided, flash drought events are
spatially assigned to municipalities and statistics are aggregated.
If \code{NULL}, returns the raw event data frame for Brazil filtered to
the requested years.}

\item{aggregate_by}{Character. Temporal resolution of the output when
\code{municipalities} is provided. One of \code{"year"} (default) or \code{"month"}.
Annual aggregation counts flash drought events and their total duration
per municipality per year.}

\item{brazil_only}{Logical. Filter the global dataset to Brazil's bounding
box (-75 to -28 lon, -35 to 6 lat) before processing. Default \code{TRUE}.}

\item{use_cache}{Logical. Reuse cached files. Default \code{TRUE}.}

\item{cache_dir}{Character. Root cache directory.
Default \code{"~/.climasus4r_cache/smvi"}.}

\item{lang}{Character. Message language: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
\itemize{
\item If \code{municipalities} is provided: a \code{climasus_df} with columns:
\itemize{
\item \code{code_muni} — 7-digit IBGE municipality code
\item \code{date} — Date (Jan 1 for annual; first day of month for monthly)
\item \code{n_fd_events} — integer count of flash drought events
\item \code{fd_total_days} — total days under flash drought
\item \code{fd_mean_severity} — mean severity (SV) across events
\item \code{fd_max_severity} — maximum severity (SV) in the period
}
Metadata: \code{stage = "climate"}, \code{type = "smvi"}.
\item If \code{municipalities = NULL}: a \code{climasus_df} with raw event data
columns: \code{cell_id}, \code{Lon}, \code{Lat}, \code{fstdate}, \code{lstdate}, \code{SV},
\code{duration_days}.
}
}
\description{
\code{sus_grid_smvi()} downloads the global flash drought inventory based on
the Soil Moisture Volatility Index (SMVI; Osman et al., 2024), spatially
assigns events to Brazilian municipalities, and returns annual or monthly
flash drought statistics as a \code{climasus_df} compatible with
\code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}} and \code{\link[=sus_mod_vulnerability_index]{sus_mod_vulnerability_index()}}.

\strong{What is SMVI?} SMVI detects \emph{flash droughts} — rapid, high-impact soil
moisture deficits that develop within days to weeks. Unlike SPI/SPEI/PDSI
(which measure cumulative drought severity), SMVI captures the \emph{speed} of
soil moisture decline by comparing a 5-day running average of root-zone
soil moisture (0–100 cm) against a 20-day running average. A flash drought
event begins when the 5-day average drops below the 20-day average AND
reaches the 20th percentile deficit threshold.

\strong{Why SMVI is different from SPI/SPEI/PDSI:}
\itemize{
\item SPI/SPEI/PDSI measure \emph{how dry} a period is (severity)
\item SMVI measures \emph{how fast} a drought develops (onset speed) —
flash droughts can devastate crops, trigger wildfires, and
compromise water supply with little warning
}

\strong{Health connections in Brazil:}
\itemize{
\item \strong{Malnutrition & food insecurity}: flash droughts destroy crops
within weeks; 1–3 month lag to increased child malnutrition in
semi-arid Northeast and Amazônia
\item \strong{Leptospirosis}: rapid soil rewetting after flash drought
events concentrates rodents near water, increasing transmission;
1–4 week lag
\item \strong{Leishmaniasis}: drought-driven human migration to water
sources increases sandfly contact; 2-year lag documented in Bahia
\item \strong{Waterborne disease}: water shortage during flash drought
increases dependence on unclean sources
}

\strong{Data source}: Global Flash Drought Inventory (Osman et al., 2024).
Based on NASA GLDAS-2 root-zone soil moisture (1990–2021). No
authentication required. Events file (109 MB) covers the full global
domain at 0.25° resolution and is downloaded once and cached.
}
\section{Algorithm}{

SMVI flash drought detection (Osman et al., 2024):
\enumerate{
\item Compute 5-day and 20-day running averages of root-zone soil
moisture (RZSM, 0–100 cm) from NASA GLDAS-2 data.
\item A flash drought \emph{onset} occurs when the 5-day average drops
below the 20-day average.
\item A flash drought \emph{event} is confirmed when the RZSM deficit
relative to the 20th percentile threshold is exceeded.
\item Severity (SV) quantifies the cumulative soil moisture deficit
during the event.
}
}

\section{Data source}{

Osman, M. et al. (2024). A global flash drought inventory based on soil
moisture volatility. \emph{Scientific Data}, 11, 916.
\doi{10.1038/s41597-024-03809-9}\cr
Data: \url{https://www.hydroshare.org/resource/642ff72592404a17bb85a8a92b4dbcd6/}
(CC BY-SA 4.0, no authentication required)
}

\examples{
\dontrun{
library(geobr)
mt_mun <- read_municipality(code_muni = "MT", year = 2020)

# Annual flash drought statistics for Mato Grosso
smvi_mt <- sus_grid_smvi(
  years          = 2000:2021,
  municipalities = mt_mun,
  aggregate_by   = "year",
  lang           = "pt"
)
sus_meta(smvi_mt, "stage")  # "climate"
sus_meta(smvi_mt, "type")   # "smvi"

# Monthly resolution
smvi_mo <- sus_grid_smvi(
  years          = 2015:2021,
  municipalities = mt_mun,
  aggregate_by   = "month"
)

# Join to health data for DLNM
combined <- sus_grid_join(sih_mt, smvi_mt)
}

}
\seealso{
\code{\link[=sus_climate_compute_spi]{sus_climate_compute_spi()}}, \code{\link[=sus_climate_compute_spei]{sus_climate_compute_spei()}},
\code{\link[=sus_grid_pdsi]{sus_grid_pdsi()}}, \code{\link[=sus_grid_join]{sus_grid_join()}}, \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}
}
