% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_spacetime_bayes.R
\name{sus_mod_spacetime_bayes}
\alias{sus_mod_spacetime_bayes}
\title{Bayesian Spatiotemporal Hierarchical Model with INLA}
\usage{
sus_mod_spacetime_bayes(
  df,
  outcome,
  W,
  time_col = "date",
  time_unit = c("year", "month", "week", "auto"),
  covariates = NULL,
  offset = NULL,
  family = c("poisson", "nbinomial", "binomial", "gaussian"),
  spatial_model = c("bym2", "bym", "besag", "iid"),
  temporal_model = c("rw1", "rw2", "ar1", "iid_time"),
  interaction_type = c("none", "I", "II", "III", "IV"),
  pc_prior_u = 0.5,
  pc_prior_alpha = 0.01,
  compute_waic = TRUE,
  compute_cpo = FALSE,
  exceedance_threshold = 1,
  n_samples = 1000L,
  seed = 42L,
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{df}{A \code{data.frame} or \code{climasus_df} at the \code{aggregate}, \code{spatial}, or
\code{climate} pipeline stage. Must contain \code{code_muni} (7-digit IBGE code),
the \code{outcome} column, the \code{time_col} column, all columns named in
\code{covariates}, and optionally an \code{offset} column. The data must form a
panel (one row per area-time combination).}

\item{outcome}{Character. Name of the response (count) column in \code{df}.}

\item{W}{A \code{climasus_weights} object produced by
\code{\link[=sus_mod_spatial_weights]{sus_mod_spatial_weights()}}. Must contain either a \verb{$W} dense matrix or
a \verb{$nb} neighbour list used to build the INLA graph.}

\item{time_col}{Character. Name of the column containing time information.
Can be a \code{Date}, \code{integer}, or \code{character} column. Default \code{"date"}.}

\item{time_unit}{Character. Temporal aggregation unit used to build the
integer time index. One of \code{"year"}, \code{"month"}, \code{"week"}, or \code{"auto"}
(automatic detection based on date range). Default \code{"auto"}.}

\item{covariates}{Character vector of covariate column names in \code{df} to
include as fixed effects. Default \code{NULL} (intercept-only model).}

\item{offset}{Character. Name of a \strong{population or expected counts} column
in \code{df} (natural scale). Entered as \code{log(offset)} in the linear
predictor. Default \code{NULL}.}

\item{family}{Character. Likelihood family. One of \code{"poisson"} (default),
\code{"nbinomial"} (negative binomial), \code{"binomial"}, or \code{"gaussian"}.}

\item{spatial_model}{Character. Spatial random effect structure. One of
\code{"bym2"} (default), \code{"bym"}, \code{"besag"}, or \code{"iid"}. See \strong{Spatial model
options}.}

\item{temporal_model}{Character. Temporal random effect structure. One of
\code{"rw1"} (default), \code{"rw2"}, \code{"ar1"}, or \code{"iid_time"}.}

\item{interaction_type}{Character. Space-time interaction type following
Knorr-Held (2000). One of \code{"none"} (default), \code{"I"}, \code{"II"}, \code{"III"},
or \code{"IV"}. See \strong{Knorr-Held interaction types}.}

\item{pc_prior_u}{Positive numeric. PC prior parameter \eqn{u} for the
BYM2 spatial precision: \eqn{P(\sigma > u) = \alpha}. Default \code{0.5}.}

\item{pc_prior_alpha}{Numeric in (0, 1). PC prior parameter \eqn{\alpha}
(tail probability). Default \code{0.01}.}

\item{compute_waic}{Logical. If \code{TRUE} (default), compute WAIC via
\code{control.compute = list(waic = TRUE)}.}

\item{compute_cpo}{Logical. If \code{TRUE}, compute CPO/PIT values via
\code{control.compute = list(cpo = TRUE)}. Default \code{FALSE} (slower).}

\item{exceedance_threshold}{Positive numeric. Compute \eqn{P(RR > threshold)}
for each space-time cell using the posterior marginals. Default \code{1.0}.}

\item{n_samples}{Positive integer. Number of posterior samples drawn via
\code{INLA::inla.posterior.sample()} to compute RR credible intervals and
exceedance probabilities. Default \code{1000L}.}

\item{seed}{Integer. Random seed for reproducibility. Default \code{42L}.}

\item{lang}{Character. Language for CLI messages: \code{"pt"} (default),
\code{"en"}, \code{"es"}.}

\item{verbose}{Logical. If \code{TRUE} (default), emit progress messages via
\strong{cli}.}
}
\value{
A named list of class \code{c("climasus_spacetime_bayes", "list")} with
the following slots:
\describe{
\item{\verb{$fixed}}{\code{data.frame} with columns \code{term}, \code{mean}, \code{sd},
\code{lower95}, \code{upper95}, \code{mode} (posterior fixed-effect summaries).}
\item{\verb{$rr}}{\code{data.frame} with columns \code{code_muni}, \code{time_idx},
\code{rr_mean}, \code{rr_lower95}, \code{rr_upper95}, \code{p_exceed} (relative risk
per space-time cell with exceedance probability).}
\item{\verb{$spatial_re}}{\code{data.frame} with columns \code{code_muni}, \code{phi_mean},
\code{phi_sd} (posterior summaries of structured spatial random effect).}
\item{\verb{$temporal_re}}{\code{data.frame} with columns \code{time_idx}, \code{psi_mean},
\code{psi_sd} (posterior summaries of structured temporal random effect).}
\item{\verb{$interaction_re}}{\code{data.frame} with columns \code{code_muni},
\code{time_idx}, \code{gamma_mean}, \code{gamma_sd}, or \code{NULL} if
\code{interaction_type = "none"}.}
\item{\verb{$fitted}}{Numeric vector of posterior mean fitted values
(response scale).}
\item{\verb{$waic}}{Named list with \verb{$waic} (scalar) and \verb{$p_eff} (effective
number of parameters), or \code{NULL} if \code{compute_waic = FALSE}.}
\item{\verb{$dic}}{Named list with \verb{$dic} (scalar) and \verb{$p_d} (effective
parameters), or \code{NULL}.}
\item{\verb{$model_spec}}{Named list with \verb{$spatial_model},
\verb{$temporal_model}, \verb{$interaction_type}, \verb{$family}.}
\item{\verb{$call}}{The matched call.}
\item{\verb{$n_areas}}{Integer. Number of unique municipalities.}
\item{\verb{$n_times}}{Integer. Number of unique time periods.}
}
}
\description{
Fits a full Bayesian spatiotemporal hierarchical model using INLA
(Integrated Nested Laplace Approximation). The model decomposes disease
risk into structured spatial effects (BYM2, BYM, Besag, or IID), structured
temporal effects (RW1, RW2, AR1, or IID), and optionally a space-time
interaction following the four Knorr-Held (2000) interaction types.
Climate and environmental covariates from a \code{climasus_df} can be included
as fixed effects.
}
\section{Model structure}{


The linear predictor for area \eqn{i} at time \eqn{t} is:

\deqn{\eta_{it} = \alpha + \mathbf{x}_{it}^{\top}\boldsymbol{\beta}
  + \phi_i + \psi_t + \delta_{it} + \log(E_{it})}

where \eqn{\phi_i} is the spatial random effect, \eqn{\psi_t} the temporal
random effect, \eqn{\delta_{it}} the optional space-time interaction, and
\eqn{E_{it}} the expected counts offset.
}

\section{Spatial model options}{


\describe{
\item{\code{"bym2"}}{BYM2 model (Riebler et al. 2016): scaled ICAR + IID with
a single precision and a mixing parameter. Recommended for disease
mapping. PC priors applied.}
\item{\code{"bym"}}{Original BYM model (Besag, York & Mollie 1991): ICAR +
independent Gaussian components with separate precision parameters.}
\item{\code{"besag"}}{Intrinsic CAR (Besag 1974): purely structured spatial
random effect.}
\item{\code{"iid"}}{Independent normal random effects (no spatial structure).}
}
}

\section{Knorr-Held interaction types}{


\describe{
\item{\code{"none"}}{No space-time interaction.}
\item{\code{"I"}}{IID interaction: \eqn{\delta_{it} \sim N(0, \sigma^2_\delta)}.}
\item{\code{"II"}}{Structured in time, unstructured in space:
separate RW1 per area.}
\item{\code{"III"}}{Structured in space, unstructured in time:
separate ICAR per time period.}
\item{\code{"IV"}}{Structured in both space and time (inseparable interaction).}
}
}

\section{PC priors}{


For the BYM2 spatial model, penalised-complexity (PC) priors are used
for the precision hyperparameter: \eqn{P(\sigma > u) = \alpha} where
\code{pc_prior_u} and \code{pc_prior_alpha} control the tail probability. Default
values \code{u = 0.5}, \code{alpha = 0.01} are weakly informative and suitable for
standardised disease mapping.
}

\examples{
\dontrun{
library(climasus4r)
library(geobr)

# Build spatial weights for Rio Grande do Norte
shp <- geobr::read_municipality(code_muni = "RN", year = 2022)
W   <- sus_mod_spatial_weights(shp, return_matrix = TRUE)

# Prepare aggregated health-climate panel data (areas x time periods)
# agg <- sus_data_aggregate(cleaned_df) |>
#          sus_climate_inmet() |>
#          sus_climate_aggregate(temporal_strategy = "exact")

# BYM2 spatiotemporal model with Poisson family
result <- sus_mod_spacetime_bayes(
  df               = agg,
  outcome          = "deaths",
  W                = W,
  time_col         = "date",
  time_unit        = "month",
  covariates       = c("temp_mean", "prec_total"),
  offset           = "population",
  family           = "poisson",
  spatial_model    = "bym2",
  temporal_model   = "rw1",
  interaction_type = "I",
  pc_prior_u       = 0.5,
  pc_prior_alpha   = 0.01,
  compute_waic     = TRUE,
  n_samples        = 1000L,
  seed             = 42L,
  lang             = "pt"
)

print(result)
result$fixed
result$rr
result$spatial_re
result$temporal_re
result$waic
}

}
\references{
Knorr-Held, L. (2000). Bayesian modelling of inseparable space-time
variation in disease risk. \emph{Statistics in Medicine}, 19(17-18),
2555-2567.

Riebler, A., Sorbye, S. H., Simpson, D., & Rue, H. (2016). An intuitive
Bayesian spatial model for disease mapping that accounts for scaling.
\emph{Statistical Methods in Medical Research}, 25(4), 1145-1165.

Rue, H., Martino, S., & Chopin, N. (2009). Approximate Bayesian inference
for latent Gaussian models using integrated nested Laplace approximations.
\emph{Journal of the Royal Statistical Society B}, 71(2), 319-392.

Blangiardo, M., & Cameletti, M. (2015). \emph{Spatial and Spatio-temporal
Bayesian Models with R-INLA}. Wiley.

Besag, J., York, J., & Mollie, A. (1991). Bayesian image restoration, with
two applications in spatial statistics. \emph{Annals of the Institute of
Statistical Mathematics}, 43(1), 1-20.
}
\seealso{
\code{\link[=sus_mod_spatial_weights]{sus_mod_spatial_weights()}} to build the required adjacency object,
\code{\link[=sus_mod_spatial_bayes]{sus_mod_spatial_bayes()}} for purely cross-sectional spatial Bayesian
models (CARBayes), \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}} for time-series DLNM models.
}
