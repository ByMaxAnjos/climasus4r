% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_rap_targets.R
\name{sus_rap_make}
\alias{sus_rap_make}
\title{Execute a targets Pipeline from a RAP}
\usage{
sus_rap_make(
  rap,
  workers = 1L,
  reporter = "verbose",
  branching = "none",
  lang = "pt",
  ...
)
}
\arguments{
\item{rap}{A \code{rap_object}, a \code{tar_rap_object}, or a \code{character(1)} path to
a \verb{_targets.R} file. When a \code{rap_object} is passed, a temporary
\verb{_targets.R} is generated automatically.}

\item{workers}{\code{integer(1)} — Parallel workers. Default \code{1}.}

\item{reporter}{\code{character(1)} — Progress reporter for \code{tar_make()}.
Default \code{"verbose"}.}

\item{branching}{\code{character(1)} — Branching strategy when \code{rap} is a
\code{rap_object} (ignored when a file path is passed). Default \code{"none"}.}

\item{lang}{\code{character(1)} — Language for messages.}

\item{...}{Additional arguments forwarded to \code{targets::tar_make()}.}
}
\value{
A list with \verb{$results}, \verb{$rap}, and \verb{$store}.
}
\description{
Runs a \verb{_targets.R} pipeline (generated by \code{\link[=sus_rap_targets]{sus_rap_targets()}}) using
\code{targets::tar_make()}, with optional parallelism via \code{workers}.
}
\examples{
\dontrun{
rap <- sus_rap_read("pipeline_sp.R")
result <- sus_rap_make(rap, workers = 2, branching = "uf")
head(result$results)
}
}
