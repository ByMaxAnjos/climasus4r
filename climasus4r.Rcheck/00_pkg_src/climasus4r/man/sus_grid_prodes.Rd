% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_grid_prodes.R
\name{sus_grid_prodes}
\alias{sus_grid_prodes}
\title{Import PRODES Deforestation Data from TerraBrasilis for Brazilian Municipalities}
\usage{
sus_grid_prodes(
  years,
  biomes = c("Amazon", "Cerrado", "MataAtlantica", "Caatinga", "Pampa", "Pantanal"),
  uf = NULL,
  municipalities = NULL,
  use_cache = TRUE,
  cache_dir = "~/.climasus4r_cache/prodes",
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{years}{Integer vector. Years to download. Availability by biome:
Amazon 2007–present; all others 2000–present.}

\item{biomes}{Character vector. Biomes to include. Any combination of:
\code{"Amazon"}, \code{"Cerrado"}, \code{"MataAtlantica"}, \code{"Caatinga"}, \code{"Pampa"},
\code{"Pantanal"}. Default: all six biomes.}

\item{uf}{Character vector. Optional state filter (e.g., \code{c("MT", "PA")}).
Reduces download size. \code{NULL} = all states in the selected biome(s).}

\item{municipalities}{An \code{sf} POLYGON object (e.g., from
\code{\link[geobr:read_municipality]{geobr::read_municipality()}}). When provided, deforestation polygons are
spatially intersected with municipality boundaries and annual areas are
aggregated. If \code{NULL}, raw deforestation polygons are returned as an sf
\code{climasus_df}.}

\item{use_cache}{Logical. Reuse previously downloaded GeoJSON files and
aggregated Parquet caches. Default \code{TRUE}.}

\item{cache_dir}{Character. Root cache directory.
Default \code{"~/.climasus4r_cache/prodes"}.}

\item{lang}{Character. Message language: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}
}
\value{
\itemize{
\item If \code{municipalities} is provided: a \code{climasus_df} with columns
\code{code_muni} (character), \code{date} (Date, Jan 1 of the PRODES year),
\code{year} (integer), \code{deforested_area_km2} (numeric, total area of
deforestation polygons intersecting the municipality), \code{n_patches}
(integer, count of distinct deforestation patches), and \code{biome}
(character). Metadata: \code{stage = "climate"}, \code{type = "prodes"}.
\item If \code{municipalities = NULL}: the raw deforestation polygon sf object
as a \code{climasus_df} with columns \code{year}, \code{state}, \code{area_km}, \code{biome}.
}
}
\description{
\code{sus_grid_prodes()} downloads INPE PRODES annual deforestation data from the
TerraBrasilis WFS API, spatially intersects deforestation polygons with
municipality boundaries, and returns municipality-level annual deforestation
areas as a \code{climasus_df} compatible with \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}} and
\code{\link[=sus_climate_anomaly]{sus_climate_anomaly()}}.

PRODES (Projeto de Monitoramento do Desmatamento na Amazônia Legal por
Satélite) is INPE's primary deforestation monitoring program. Deforestation
exposure is a key driver of frontier malaria (SINAN), dengue, leishmaniasis,
and respiratory disease from biomass burning in Brazil.
}
\section{PRODES year convention}{

A PRODES year N covers August 1 of year N-1 through July 31 of year N.
The \code{date} column is set to \code{YYYY-01-01} by convention (the publication
year). For time-series analyses, note that most deforestation peaks occur
during the dry season (July–October).
}

\section{Data source}{

INPE PRODES via TerraBrasilis WFS. No authentication required.\cr
All biomes accessed via \url{https://terrabrasilis.dpi.inpe.br/geoserver/wfs}
}

\section{Packages required}{

\code{sf} is required for spatial intersection when \code{municipalities} is provided.
\code{httr2} and \code{jsonlite} are used for WFS downloads (in \code{Suggests}).
}

\examples{
\dontrun{
library(geobr)
mt_mun <- read_municipality(code_muni = "MT", year = 2020)

# Annual deforestation for Mato Grosso municipalities, Amazon biome
prodes <- sus_grid_prodes(
  years          = 2015:2022,
  biomes         = "Amazon",
  uf             = "MT",
  municipalities = mt_mun,
  lang           = "pt"
)
sus_meta(prodes, "stage")  # "climate"
sus_meta(prodes, "type")   # "prodes"

# Raw polygon data (no aggregation)
prodes_raw <- sus_grid_prodes(
  years  = 2022,
  biomes = "Amazon",
  uf     = "PA",
  lang   = "en"
)
}

}
\seealso{
\code{\link[=sus_grid_fires]{sus_grid_fires()}}, \code{\link[=sus_climate_anomaly]{sus_climate_anomaly()}}, \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}
}
