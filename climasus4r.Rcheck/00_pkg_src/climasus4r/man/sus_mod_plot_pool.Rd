% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_plot_pool.R
\name{sus_mod_plot_pool}
\alias{sus_mod_plot_pool}
\title{Plots and Tables from a Pooled DLNM Meta-Analysis}
\usage{
sus_mod_plot_pool(
  x,
  type = c("overall", "forest", "spaghetti"),
  output_type = c("plot", "table", "all"),
  interactive = FALSE,
  base_size = 12L,
  save_plot = NULL,
  lang = c("pt", "en", "es"),
  verbose = FALSE
)
}
\arguments{
\item{x}{A \code{climasus_pool} object produced by \code{\link[=sus_mod_pool]{sus_mod_pool()}}.}

\item{type}{Character. Plot type: \code{"overall"} (default), \code{"forest"}, or
\code{"spaghetti"}.}

\item{output_type}{Character. \code{"plot"} (default), \code{"table"}, or \code{"all"}
(named list \verb{$plot}, \verb{$table}, \verb{$data}).}

\item{interactive}{Logical. \code{TRUE} converts the ggplot2 output to an
interactive \pkg{plotly} widget. Default \code{FALSE}.}

\item{base_size}{Numeric. ggplot2 base font size. Default \code{12}.}

\item{save_plot}{Character or \code{NULL}. File path to save the plot.}

\item{lang}{Character. Language for labels: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{FALSE}.}
}
\value{
Depending on \code{output_type}:
\itemize{
\item \code{"plot"} -> a \code{ggplot} or \code{plotly} object.
\item \code{"table"} -> a \code{tibble} of the plotted data.
\item \code{"all"} -> a named list: \verb{$plot}, \verb{$table}, \verb{$data}.
}
}
\description{
Produces exposure-response curves, city-specific forest plots, and BLUP
spaghetti plots from a \code{climasus_pool} object returned by \code{\link[=sus_mod_pool]{sus_mod_pool()}}.
}
\section{Plot types (\code{type})}{
\tabular{ll}{
   \code{type} \tab Description \cr
   \code{"overall"} \tab Pooled exposure-response curve with CI ribbon \cr
   \code{"forest"} \tab City-specific RR at exposure p75 with CI (original and BLUP) \cr
   \code{"spaghetti"} \tab Per-city BLUP curves overlaid on the pooled curve \cr
}
}

\examples{
\dontrun{
pool <- sus_mod_pool(fits, lang = "pt")

sus_mod_plot_pool(pool, type = "overall",   lang = "pt")
sus_mod_plot_pool(pool, type = "forest",    lang = "en")
sus_mod_plot_pool(pool, type = "spaghetti", lang = "en")
out <- sus_mod_plot_pool(pool, output_type = "all")
out$table
}

}
\seealso{
\code{\link[=sus_mod_pool]{sus_mod_pool()}}, \code{\link[=sus_mod_plot_dlnm]{sus_mod_plot_dlnm()}}
}
