% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_rap_gui.R
\name{sus_rap_gui}
\alias{sus_rap_gui}
\title{Launch an Interactive GUI for Editing and Running RAPs}
\usage{
sus_rap_gui(rap = NULL, launch.browser = TRUE, ...)
}
\arguments{
\item{rap}{A \code{rap_object} (from \code{\link[=sus_rap_read]{sus_rap_read()}}) to pre-populate the
interface. Pass \code{NULL} (default) to start from a blank template.}

\item{launch.browser}{\code{logical(1)} — Open in the system browser.
Default \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{shiny::runApp()}.}
}
\value{
Does not return; runs the Shiny application interactively.
}
\description{
Opens a Shiny application that lets users visually configure pipeline
parameters (UF, years, system, aggregation), preview the generated code,
and execute or export the RAP — all without writing R code directly.
}
\section{Interface panels}{

\describe{
\item{Left}{Parameter editors: UF checkboxes, year range slider, system
dropdown, aggregation unit selector, language selector.}
\item{Center}{Step-by-step pipeline card view showing each function and
its arguments.}
\item{Right}{Live log console. Buttons: \strong{Preview}, \strong{Run}, \strong{Export},
\strong{Save Recipe}.}
}
}

\examples{
\dontrun{
sus_rap_gui()

rap <- sus_rap_read("pipeline_sp.R")
sus_rap_gui(rap)
}
}
