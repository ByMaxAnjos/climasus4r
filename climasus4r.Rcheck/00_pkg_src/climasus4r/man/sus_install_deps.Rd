% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{sus_install_deps}
\alias{sus_install_deps}
\title{Install Optional Dependencies for climasus4r}
\usage{
sus_install_deps(features = "all", force = FALSE, lang = "pt")
}
\arguments{
\item{features}{Character vector. Feature groups to install.
One or more of \code{"parquet"}, \code{"spatial"}, \code{"plot"}, \code{"models"}, \code{"all"}.
Default \code{"all"}.}

\item{force}{Logical. Re-install even if the package is already available.
Default \code{FALSE}.}

\item{lang}{Character. Language for messages: \code{"pt"} (default), \code{"en"},
\code{"es"}.}
}
\description{
Installs optional packages needed for specific feature groups.
Tries multiple installation strategies, including Posit Package Manager
(PPM) for pre-compiled binaries on Posit Cloud / Linux.
}
\details{
\describe{
\item{\code{"parquet"}}{Installs \code{arrow} (fastest Parquet) and/or
\code{nanoparquet} (pure-R fallback, no C++ required).}
\item{\code{"spatial"}}{Installs \code{sf}, \code{geobr}, \code{geocodebr}, \code{sfarrow},
\code{censobr}.}
\item{\code{"plot"}}{Installs \code{ggplot2}, \code{plotly}, \code{patchwork}, \code{ggsci}.}
\item{\code{"models"}}{Installs \code{dlnm}, \code{mvmeta}, \code{survival}, \code{xgboost}.}
}

On Posit Cloud, PPM binaries are used automatically for faster installation.
}
\examples{
\dontrun{
# Install all optional dependencies
sus_install_deps()

# Install only Parquet backend (no C++ required path)
sus_install_deps("parquet")

# Force reinstall arrow
sus_install_deps("parquet", force = TRUE)
}

}
