% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_grid_fires.R
\name{sus_grid_fires}
\alias{sus_grid_fires}
\title{Import Fire Hotspot Data for Brazilian Municipalities}
\usage{
sus_grid_fires(
  years,
  months = 1:12,
  uf = NULL,
  bbox = NULL,
  source = c("inpe", "firms_modis", "firms_viirs"),
  municipalities = NULL,
  agg_fun = "count",
  biome = NULL,
  use_cache = TRUE,
  cache_dir = "~/.climasus4r_cache/fires",
  firms_key = NULL,
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{years}{Numeric vector of years. INPE: 1998–present.
FIRMS MODIS: 2000–present. FIRMS VIIRS: 2012–present.}

\item{months}{Integer vector (1–12). Default \code{1:12}.}

\item{uf}{Character vector of state codes (e.g., \code{c("MT", "PA")}).
Case-insensitive. \code{NULL} = all Brazil. Used to filter INPE queries and
as a shortcut to define \code{bbox} for FIRMS.}

\item{bbox}{Numeric vector \code{c(xmin, ymin, xmax, ymax)} in WGS84 longitude/
latitude. Used by FIRMS for spatial subsetting. If \code{NULL} and \code{uf} is
provided, the bbox is computed from Brazilian state centroids. If both
are \code{NULL}, Brazil's bounding box is used.}

\item{source}{Character. Data source: \code{"inpe"} (default), \code{"firms_modis"},
or \code{"firms_viirs"}.}

\item{municipalities}{An \code{sf} POLYGON object (e.g., from
\code{\link[geobr:read_municipality]{geobr::read_municipality()}}). When provided, fire points are spatially
joined to polygons and aggregated to daily municipality-level counts.
If \code{NULL}, the raw fire point data are returned as a \code{climasus_df}.}

\item{agg_fun}{Character. Aggregation strategy when \code{municipalities} is
provided. Currently \code{"count"} is supported (count of fire hotspots per
municipality per day). Future versions may support \code{"frp_sum"}.}

\item{biome}{Character vector. INPE biome filter. Allowed values:
\code{"Amazonia"}, \code{"Cerrado"}, \code{"Mata Atlantica"}, \code{"Caatinga"},
\code{"Pampa"}, \code{"Pantanal"}. \code{NULL} = no filter.}

\item{use_cache}{Logical. Reuse previously downloaded CSV/JSON files.
Default \code{TRUE}.}

\item{cache_dir}{Character. Root cache directory.
Default \code{"~/.climasus4r_cache/fires"}.}

\item{firms_key}{Character. NASA FIRMS MAP KEY.
Defaults to \code{Sys.getenv("FIRMS_MAP_KEY")}. Required when
\code{source \%in\% c("firms_modis", "firms_viirs")}.}

\item{lang}{Character. Message language: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}
}
\value{
\itemize{
\item If \code{municipalities} is provided: a \code{climasus_df} with columns
\code{date} (Date), \code{code_muni} (character), \code{n_fires} (integer count of
hotspots), and \code{frp_mean} (mean Fire Radiative Power in MW,
\code{NA} when not available). Metadata: \code{stage = "climate"},
\code{type = "fires"}.
\item If \code{municipalities = NULL}: a \code{climasus_df} with raw fire point
columns: \code{date}, \code{lat}, \code{lon}, \code{frp}, \code{biome}, \code{estado}, \code{source}.
}
}
\description{
\code{sus_grid_fires()} downloads fire hotspot (active fire) data from INPE
Queimadas or NASA FIRMS, optionally aggregates counts and Fire Radiative
Power (FRP) to Brazilian municipalities, and returns a \code{climasus_df}
compatible with \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}} and \code{\link[=sus_climate_anomaly]{sus_climate_anomaly()}}.

Fire data is critical for respiratory health analyses in Brazil: smoke from
agricultural burning (cerrado, cana-de-acucar) and deforestation fires
(Amazonia) are associated with acute respiratory outcomes in SIH/SINAN data.

\strong{Sources available:}
\itemize{
\item \code{"inpe"} (default) — INPE Queimadas portal. No authentication.
Historical data from 1998 to present. Brazil only.
DOI: \doi{10.2312/inpe.2022.009}
\item \code{"firms_modis"} — NASA FIRMS MODIS Collection 6.1.
Requires a free MAP KEY from
\url{https://firms.modaps.eosdis.nasa.gov/api/map_key/}.
Global, 1 km resolution, 2000 to present.
\item \code{"firms_viirs"} — NASA FIRMS VIIRS SNPP.
Same key as FIRMS MODIS. 375 m resolution, 2012 to present.
}
}
\section{INPE Queimadas API}{

Queries \verb{https://queimadas.dgi.inpe.br/api/focos/} monthly. Each monthly
request is cached as a JSON file. Large states in fire-season months may
return truncated results due to the API's record limit; use \code{uf} to
narrow the query when needed.
}

\section{NASA FIRMS API}{

Queries \verb{https://firms.modaps.eosdis.nasa.gov/api/area/csv/\{key\}/\{product\}/\{bbox\}/10/\{date\}}
in 10-day chunks (maximum allowed per request). A free MAP KEY is required
and must be activated at \url{https://firms.modaps.eosdis.nasa.gov/api/map_key/}.
}

\examples{
\dontrun{
library(geobr)
mt_mun <- read_municipality(code_muni = "MT", year = 2020)

# INPE fire count for Mato Grosso municipalities, fire season 2020
fires <- sus_grid_fires(
  years          = 2020,
  months         = 7:10,
  uf             = "MT",
  municipalities = mt_mun,
  lang           = "pt"
)
sus_meta(fires, "stage")  # "climate"
sus_meta(fires, "type")   # "fires"

# Raw fire points for Amazonia, no aggregation
fires_pts <- sus_grid_fires(
  years  = 2022,
  months = 8,
  biome  = "Amazonia",
  lang   = "en"
)

# FIRMS MODIS with API key
Sys.setenv(FIRMS_MAP_KEY = "my_key")
fires_firms <- sus_grid_fires(
  years          = 2023,
  months         = 9,
  uf             = "MT",
  source         = "firms_modis",
  municipalities = mt_mun
)
}

}
\seealso{
\code{\link[=sus_grid_pollution_cams]{sus_grid_pollution_cams()}}, \code{\link[=sus_climate_anomaly]{sus_climate_anomaly()}},
\code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}
}
