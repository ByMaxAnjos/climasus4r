% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_casecrossover.R
\name{sus_mod_casecrossover}
\alias{sus_mod_casecrossover}
\title{Time-Stratified Case-Crossover Analysis for Climate-Health Data}
\usage{
sus_mod_casecrossover(
  data,
  outcome_col = "n_obitos",
  exposure_col,
  covariates = NULL,
  stratum = "month",
  lag = 0L,
  method = "conditional_poisson",
  family = "quasipoisson",
  alpha = 0.05,
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{climasus_df} at stage \code{"aggregate"} or \code{"climate"} (produced
by \code{sus_climate_aggregate()}), or any \code{data.frame} containing at minimum a
\code{date} column (Date), an outcome count column, and an exposure column.}

\item{outcome_col}{Character. Name of the daily health outcome count column
(e.g., \code{"n_obitos"}, \code{"n_internacoes"}). Default: \code{"n_obitos"}.}

\item{exposure_col}{Character. Name of the exposure column. Required; no
auto-detection is performed (unlike \code{sus_mod_dlnm()}). Examples:
\code{"tair_dry_bulb_c"}, \code{"pm25_mean"}, \code{"precip_mm"}.}

\item{covariates}{Character vector or \code{NULL}. Names of additional columns to
include as linear confounders. These should be time-varying within strata
(e.g., day-of-week dummy, holiday indicator, relative humidity). Long-term
trends are already controlled by the strata. Default: \code{NULL}.}

\item{stratum}{Character. Time stratum type or name of an existing column:
\itemize{
\item \code{"month"} (default): year-month strata (e.g., \code{"2021-07"}).
\item \code{"week"}: ISO year-week strata (e.g., \code{"2021-W27"}).
\item Any column name in \code{data}: uses that column's values as stratum IDs.
}}

\item{lag}{Integer or integer vector. Lag(s) to apply to the exposure before
fitting. A single integer uses that specific lag; a vector uses the mean
of exposures at all specified lags. Default: \code{0L} (same-day exposure).}

\item{method}{Character. Fitting method:
\itemize{
\item \code{"conditional_poisson"} (default): GLM with stratum fixed effects.
Preferred for aggregate count data; uses \code{stats::glm()} with the
specified \code{family}.
\item \code{"clogit"}: conditional logistic regression via \code{survival::clogit()}.
Requires the \code{survival} package. Treats outcome as binary
(\code{is_case = count > 0}); best for rare events.
}}

\item{family}{Character. GLM family for \code{method = "conditional_poisson"}:
\code{"quasipoisson"} (default, robust to overdispersion) or \code{"poisson"}.
Ignored for \code{method = "clogit"}.}

\item{alpha}{Numeric. Significance level for confidence intervals.
Default: \code{0.05} (95\% CI).}

\item{lang}{Character. Language for messages: \code{"pt"} (default), \code{"en"},
\code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{climasus_casecrossover} list with:
\describe{
\item{\verb{$model}}{The fitted model object: a \code{glm} for
\code{"conditional_poisson"} or a \code{clogit} object for \code{"clogit"}.}
\item{\verb{$or_table}}{Tibble with one row per exposure term:
\code{term}, \code{lag_spec}, \code{estimate} (log-OR), \code{or}, \code{or_lo}, \code{or_hi},
\code{p_value}.}
\item{\verb{$data}}{The analysis dataset after lag creation and NA removal:
columns \code{date}, \code{outcome_val}, \code{exposure_val}, \code{stratum_id}, and
any covariates.}
\item{\verb{$diagnostics}}{List: \code{n_obs}, \code{n_cases}, \code{n_strata},
\code{disp_ratio} (for \code{"conditional_poisson"}), \code{method}, \code{family}.}
\item{\verb{$meta}}{List of analysis parameters: \code{outcome_col},
\code{exposure_col}, \code{covariates}, \code{stratum}, \code{lag}, \code{method},
\code{family}, \code{alpha}, \code{call_time}.}
}
}
\description{
Fits a time-stratified case-crossover model to quantify the association
between a climate exposure and a daily health outcome. Each time stratum
(month or week) acts as its own control, removing long-term trends and
seasonal confounding by design. Two fitting methods are supported:
\code{"conditional_poisson"} (GLM with stratum fixed effects, default) for
aggregate count data, and \code{"clogit"} (conditional logistic regression) for
binary outcomes or rare events.
}
\section{Statistical framework}{


\strong{\code{"conditional_poisson"} method} (Whitaker et al., 2006):

\deqn{\log(E[Y_t]) = \alpha_s + \beta X_t + \mathbf{z}_t^\top \boldsymbol{\gamma}}

where \eqn{\alpha_s} is a stratum-specific intercept (absorbed as
\code{factor(stratum_id)} in the GLM), \eqn{X_t} is the (possibly lagged or
averaged) exposure on day \eqn{t}, and \eqn{\mathbf{z}_t} are optional
time-varying covariates. The rate ratio is \eqn{RR = e^\beta}.

\strong{\code{"clogit"} method} (Maclure, 1991; Levy et al., 2001):

Uses \code{survival::clogit()} with the binary indicator
\eqn{D_t = \mathbf{1}(Y_t > 0)} as the outcome. Each case day is matched
to all other days within the same stratum as controls. This method is most
appropriate for rare events (low daily counts) and binary outcomes.
}

\section{Lag specification}{


The \code{lag} argument controls how the daily exposure is lagged before the
association is estimated:
\itemize{
\item \strong{Single integer} (e.g., \code{lag = 0}): uses the exposure exactly \code{lag} days
before the outcome day.
\item \strong{Integer vector} (e.g., \code{lag = 0:6}): uses the arithmetic mean of
exposures at all specified lags — a moving average common for temperature
(e.g., \code{lag = 0:6} is the "lag 0-6" or "mean temperature over 7 days").
}
}

\examples{
\dontrun{
# Conditional Poisson with lag-0 temperature
cc <- sus_mod_casecrossover(
  df_daily,
  outcome_col  = "n_obitos",
  exposure_col = "tair_dry_bulb_c",
  stratum      = "month",
  lag          = 0L,
  lang         = "pt"
)
print(cc)
tidy(cc)

# Moving-average lag 0-6
cc_lag06 <- sus_mod_casecrossover(
  df_daily,
  outcome_col  = "n_obitos",
  exposure_col = "tair_dry_bulb_c",
  lag          = 0L:6L,
  lang         = "en"
)
}

}
\references{
Maclure, M. (1991). The case-crossover design: a method for studying
transient effects on the risk of acute events. \emph{American Journal of
Epidemiology}, 133(2), 144-153. \doi{10.1093/oxfordjournals.aje.a115853}

Levy, D., Lumley, T., Sheppard, L., Kaufman, J., & Checkoway, H. (2001).
Referent selection in case-crossover analyses of acute health effects of
air pollution. \emph{Epidemiology}, 12(2), 186-192.

Whitaker, H.J., Farrington, C.P., Spiessens, B., & Musonda, P. (2006).
Tutorial in biostatistics: the self-controlled case series method.
\emph{Statistics in Medicine}, 25(10), 1768-1797. \doi{10.1002/sim.2302}

Armstrong, B.G., Gasparrini, A., & Tobias, A. (2014). Conditional
Poisson models: a flexible alternative to conditional logistic case
crossover analysis. \emph{BMC Medical Research Methodology}, 14, 122.
\doi{10.1186/1471-2288-14-122}
}
\seealso{
\code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}, \code{\link[=sus_mod_excess]{sus_mod_excess()}}, \code{\link[=sus_mod_af]{sus_mod_af()}}
}
