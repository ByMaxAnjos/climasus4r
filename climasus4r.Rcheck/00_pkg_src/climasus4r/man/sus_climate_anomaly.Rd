% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_climate_anomaly.R
\name{sus_climate_anomaly}
\alias{sus_climate_anomaly}
\title{Climate Anomaly Computation vs. INMET Climatological Normals}
\usage{
sus_climate_anomaly(
  observed,
  normals,
  vars = NULL,
  method = c("absolute", "relative", "standardized", "all"),
  time_scale = c("monthly", "decadal"),
  station_col = "station_code",
  date_col = "date",
  lang = c("pt", "en", "es"),
  verbose = TRUE
)
}
\arguments{
\item{observed}{A \code{climasus_df} from \code{\link[=sus_climate_inmet]{sus_climate_inmet()}} or
\code{\link[=sus_climate_aggregate]{sus_climate_aggregate()}}. At minimum must contain: a station identifier
column (see \code{station_col}), a date column (see \code{date_col}), and at least
one climate variable column covered by the \code{vars} mapping.}

\item{normals}{A \code{climasus_df} from \code{\link[=sus_climate_normals]{sus_climate_normals()}}. Must contain
columns \code{codigo}, \code{mes}, \code{decada}, \code{var_code}, and \code{valor}. Run
\code{\link[=sus_climate_normals_meta]{sus_climate_normals_meta()}} to see available \code{var_code} values.}

\item{vars}{Named character vector mapping \strong{observed column names} to
\strong{normals \code{var_code} values}, e.g.
\code{c(tair_max_c = "t_max", rainfall_mm = "precipitation")}.
\code{NULL} (default) auto-detects variables using the built-in map for
standard INMET columns.}

\item{method}{Character. Anomaly computation method: \code{"absolute"} (default),
\code{"relative"}, \code{"standardized"}, or \code{"all"}.}

\item{time_scale}{Character. Temporal resolution for the anomaly:
\code{"monthly"} (default) averages normals across the three decades of each
month and aggregates observations by calendar month; \code{"decadal"} preserves
the 10-day decade structure of INMET normals and is more precise for
variables such as temperature extremes and precipitation.}

\item{station_col}{Character. Name of the station identifier column in
\code{observed}. Default \code{"station_code"}. Must contain INMET codes matching
the \code{codigo} column in \code{normals}.}

\item{date_col}{Character. Name of the date or datetime column in
\code{observed}. Default \code{"date"}. Accepts \code{Date} or \code{POSIXct}.}

\item{lang}{Character. Language for messages: \code{"pt"} (default), \code{"en"},
\code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{climasus_df} at stage \code{"climate"} and type \code{"anomaly"}. One row
per station × year × month (or × decade when \code{time_scale = "decadal"}).
Key columns:
\describe{
\item{\code{station_col}}{Station identifier (same as \code{station_col} argument).}
\item{\code{year}}{Calendar year of the observation.}
\item{\code{month_num}}{Month number (1–12).}
\item{\code{month_name}}{Portuguese month name, ASCII.}
\item{\code{decade_num}}{Decade within month (1, 2, or 3). Present only when
\code{time_scale = "decadal"}.}
\item{\verb{\{v\}_obs}}{Observed monthly/decadal value (mean or sum depending on
variable type).}
\item{\verb{\{v\}_normal}}{Climatological normal value.}
\item{\verb{\{v\}_anomaly}}{Absolute anomaly: obs − normal.}
\item{\verb{\{v\}_anomaly_pct}}{Relative anomaly (\\%): (obs − normal) /
|normal| × 100. Only when \verb{method ∈ c("relative", "all")}.}
\item{\verb{\{v\}_anomaly_std}}{Standardized anomaly (z-score):
(obs − normal) / σ_obs, where σ is the inter-annual SD of monthly
observations for each station × month. Only when
\verb{method ∈ c("standardized", "all")}.}
}
}
\description{
Computes climate anomalies by comparing observed meteorological data from
\code{\link[=sus_climate_inmet]{sus_climate_inmet()}} against 30-year climatological normals from
\code{\link[=sus_climate_normals]{sus_climate_normals()}}. Three anomaly types are supported: absolute
(obs − normal), relative ((obs − normal) / |normal| × 100 \%), and
standardized z-score ((obs − normal) / σ_obs). The result is a \code{climasus_df}
ready for epidemiological modelling with \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}},
\code{\link[=sus_climate_compute_heatwaves]{sus_climate_compute_heatwaves()}}, or \code{\link[=sus_mod_vulnerability_index]{sus_mod_vulnerability_index()}}.
}
\section{Why anomalies matter for climate-health with DATASUS}{

\itemize{
\item \strong{Multi-city comparability}: standardized anomalies make DLNM
exposure-response curves poolable across cities with very different
baseline climates (Manaus vs. Porto Alegre).
\item \strong{Extreme event attribution}: anomaly peaks pinpoint the dates to
correlate with DATASUS hospitalization or mortality surges.
\item \strong{Climate trend detection}: a positive trend in temperature anomalies
captures local warming for the IPCC Exposure pillar in
\code{\link[=sus_mod_vulnerability_index]{sus_mod_vulnerability_index()}}.
\item \strong{Vector-borne and waterborne diseases}: precipitation anomalies
detect flood/drought episodes — with a 2–4 week lag — relevant to dengue,
leptospirosis, and diarrheal disease in SINAN.
\item \strong{Thermal comfort indices}: anomaly-adjusted temperature feeds DLNM
models without confounding by mean inter-city temperature differences.
}
}

\section{Anomaly methods}{
\tabular{lll}{
   Method \tab Formula \tab Primary use \cr
   \code{"absolute"} \tab obs − normal \tab Temperature anomalies (°C) \cr
   \code{"relative"} \tab (obs − normal) / |normal| × 100 \tab Precipitation anomaly (\%) \cr
   \code{"standardized"} \tab (obs − normal) / σ_obs \tab Multi-city pooling, DLNM input \cr
   \code{"all"} \tab All three \tab Exploratory analysis \cr
}
}

\examples{
\dontrun{
# 1. Download data
obs  <- sus_climate_inmet(years = 2018:2023, uf = "RJ")
norm <- sus_climate_normals(period = "1991-2020",
                             target_var = c("t_max", "t_min",
                                            "t_mean_comp", "precipitation"))

# 2. Monthly absolute anomalies (default)
anom <- sus_climate_anomaly(obs, norm)

# 3. Standardized anomalies for multi-city DLNM pooling
anom_std <- sus_climate_anomaly(obs, norm,
                                 method     = "standardized",
                                 vars       = c(tair_dry_bulb_c = "t_mean_comp"))

# 4. Decadal precipitation anomalies — dengue/leptospirosis lag studies
anom_10d <- sus_climate_anomaly(obs, norm,
                                 method     = "relative",
                                 time_scale = "decadal",
                                 vars       = c(rainfall_mm = "precipitation"))

# 5. All methods at once — exploratory
anom_all <- sus_climate_anomaly(obs, norm, method = "all")
}

}
\seealso{
\code{\link[=sus_climate_normals]{sus_climate_normals()}}, \code{\link[=sus_climate_normals_meta]{sus_climate_normals_meta()}},
\code{\link[=sus_climate_inmet]{sus_climate_inmet()}}, \code{\link[=sus_climate_aggregate]{sus_climate_aggregate()}},
\code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}, \code{\link[=sus_mod_vulnerability_index]{sus_mod_vulnerability_index()}}
}
