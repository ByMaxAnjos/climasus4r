% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_data_aggregate.R
\name{sus_as_duckdb}
\alias{sus_as_duckdb}
\title{Convert a climasus_df to a DuckDB lazy tbl with embedded metadata}
\usage{
sus_as_duckdb(df, con, name = "sus_data", overwrite = TRUE)
}
\arguments{
\item{df}{A \code{climasus_df} tibble.}

\item{con}{A DuckDB \code{DBIConnection} (from \code{duckdb::dbConnect(duckdb::duckdb())}).}

\item{name}{Character; name for the DuckDB table. Defaults to \code{"sus_data"}.}

\item{overwrite}{Logical. Overwrite existing table? Default \code{TRUE}.}
}
\value{
A \code{tbl_dbi} referencing the DuckDB table.
}
\description{
Copies the data to a DuckDB connection and stores \code{sus_meta} in a helper
table named \code{.__sus_meta__}.  This allows \code{sus_data_aggregate()} to
recover the metadata automatically.
}
\examples{
\dontrun{
con      <- duckdb::dbConnect(duckdb::duckdb())
duck_tbl <- sus_data_standardize(df) |> sus_as_duckdb(con)
result   <- sus_data_aggregate(duck_tbl, time_unit = "month")
}
}
