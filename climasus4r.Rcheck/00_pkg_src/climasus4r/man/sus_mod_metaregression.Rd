% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_metaregression.R
\name{sus_mod_metaregression}
\alias{sus_mod_metaregression}
\title{Meta-Regression of Pooled DLNM Estimates with City-Level Covariates}
\usage{
sus_mod_metaregression(
  fits,
  covariates,
  covariate_cols = NULL,
  city_col = NULL,
  pred_at = c(0.75, 0.9, 0.95, 0.99),
  blup = TRUE,
  method = "reml",
  alpha = 0.05,
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{fits}{A named list of \code{climasus_dlnm} objects, one per city or region.
All fits must use the same \code{climate_col}, \code{lag_max}, \code{argvar}, and
\code{arglag}. Produced by \code{sus_mod_dlnm()}.}

\item{covariates}{A \code{data.frame} with one row per city and city-level
predictors. City names must be stored as row names (default) or in the
column specified by \code{city_col}. Cities present in \code{fits} but absent in
\code{covariates} are excluded from the meta-regression with a warning.}

\item{covariate_cols}{Character vector of column names to use as
meta-regression predictors. Default \code{NULL} uses all numeric columns in
\code{covariates} (after removing the city identifier column if any).}

\item{city_col}{Character. Name of the column in \code{covariates} that holds
city identifiers. Default \code{NULL} uses row names of \code{covariates}.}

\item{pred_at}{Numeric vector of quantile probabilities (0–1) for the
exposure-response summary table. Default \code{c(0.75, 0.90, 0.95, 0.99)}.}

\item{blup}{Logical. Compute BLUP city-specific predictions? Default \code{TRUE}.}

\item{method}{Character. \code{mvmeta} estimation method: \code{"reml"} (default),
\code{"ml"}, or \code{"fixed"}.}

\item{alpha}{Numeric. Significance level for confidence intervals.
Default \code{0.05}.}

\item{lang}{Character. Language for messages: \code{"pt"} (default), \code{"en"},
\code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{climasus_metaregression} list with:
\describe{
\item{\verb{$mvmeta_fit}}{The \code{mvmeta} model with covariates.}
\item{\verb{$null_fit}}{Intercept-only \code{mvmeta} model (for heterogeneity
comparison).}
\item{\verb{$pooled_pred}}{\code{dlnm::crosspred} for the average city (all
standardized covariates = 0, i.e., the intercept).}
\item{\verb{$blup_preds}}{Named list of per-city BLUP \code{crosspred} objects
(when \code{blup = TRUE}).}
\item{\verb{$city_table}}{Tibble: one row per city with raw and BLUP RRs at
the 75th percentile exposure.}
\item{\verb{$covariate_tests}}{Tibble: one row per covariate with Wald
chi-square statistic (df = \code{n_crossbasis_coef}) and p-value.}
\item{\verb{$heterogeneity}}{Tibble comparing null and full-model heterogeneity:
Cochran Q, df, p-value, I^2, and proportion of heterogeneity
explained by covariates (R^2_het).}
\item{\verb{$cov_scales}}{Tibble with the mean and SD used to standardize
each covariate (for back-transformation).}
\item{\verb{$meta}}{Metadata: \code{climate_col}, \code{outcome_col}, \code{lag_max},
\code{n_cities}, \code{city_names}, \code{covariate_cols}, \code{method}, \code{alpha},
\code{call_time}.}
}
}
\description{
Extends two-stage multivariate meta-analysis (\code{sus_mod_pool()}) by including
city-level covariates (e.g., mean temperature, poverty index, age structure)
to explain between-city heterogeneity in climate-health associations.
Covariates are automatically standardized to mean 0, SD 1 so the intercept
represents the predicted association for an "average city". Returns
covariate Wald tests, residual heterogeneity statistics, and BLUP
predictions per city adjusted for covariate information.
}
\section{Statistical framework}{


Let \eqn{\boldsymbol{\theta}_i} be the \eqn{p}-dimensional vector of
cross-basis coefficients for city \eqn{i}, with variance-covariance
matrix \eqn{S_i}. The meta-regression model is:

\deqn{\boldsymbol{\theta}_i \sim
  N\!\left(\boldsymbol{B}_0 + \sum_{j=1}^{k} \boldsymbol{B}_j x_{ij},
  S_i + \Psi\right)}

where \eqn{\boldsymbol{B}_j} is the \eqn{p}-vector of regression
coefficients for covariate \eqn{j} and \eqn{\Psi} is the residual
between-city covariance estimated by REML. Because covariates are
standardized before fitting, \eqn{\boldsymbol{B}_0} is the predicted
association for a city with mean covariate values.

The \strong{significance} of each covariate is assessed by a multivariate Wald
test:
\deqn{W_j = \hat{\boldsymbol{B}}_j^{\top} \hat{V}_j^{-1}
  \hat{\boldsymbol{B}}_j \;\sim\; \chi^2_p}

where \eqn{\hat{V}_j} is the estimated covariance of
\eqn{\hat{\boldsymbol{B}}_j}.
}

\examples{
\dontrun{
# City-level covariates: mean annual temperature and poverty index
city_meta <- data.frame(
  mean_temp  = c(28.5, 22.1, 19.8),
  poverty_pct = c(42, 28, 15),
  row.names  = c("fortaleza", "belo_horizonte", "curitiba")
)

mr <- sus_mod_metaregression(fits, city_meta, lang = "en")
print(mr)
tidy(mr)
}

}
\references{
Gasparrini, A., et al. (2012). Multivariate meta-analysis for non-linear
and other multi-parameter associations. \emph{Statistics in Medicine}, 31(29),
3821-3839. \doi{10.1002/sim.5471}

Berkey, C.S., et al. (1998). Meta-analysis of multiple outcomes by
regression with random effects. \emph{Statistics in Medicine}, 17(22),
2537-2550.
}
\seealso{
\code{\link[=sus_mod_pool]{sus_mod_pool()}}, \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}, \code{\link[=sus_mod_sensitivity]{sus_mod_sensitivity()}}
}
