% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_rap_template.R
\name{sus_rap_template}
\alias{sus_rap_template}
\title{Scaffold a Reproducible Analytical Pipeline Project}
\usage{
sus_rap_template(
  path,
  name,
  system = "SIM-DO",
  include_renv = TRUE,
  include_targets = TRUE,
  include_quarto = TRUE,
  include_github_actions = FALSE,
  lang = "pt"
)
}
\arguments{
\item{path}{\code{character(1)} — Parent directory where \verb{<name>/} will be
created (e.g. \code{"~/projetos"}).}

\item{name}{\code{character(1)} — Project name; becomes the directory name.}

\item{system}{\code{character(1)} — DATASUS system for the template pipeline
(e.g. \code{"SIM-DO"}, \code{"SINAN"}, \code{"SIH"}). Default \code{"SIM-DO"}.}

\item{include_renv}{\code{logical(1)} — Initialise \code{renv} for dependency
management. Requires the \code{renv} package. Default \code{TRUE}.}

\item{include_targets}{\code{logical(1)} — Create \verb{_targets.R} and \code{run.R}.
Default \code{TRUE}.}

\item{include_quarto}{\code{logical(1)} — Create \code{analysis.qmd}. Default \code{TRUE}.}

\item{include_github_actions}{\code{logical(1)} — Create
\code{.github/workflows/rap.yml}. Default \code{FALSE}.}

\item{lang}{\code{character(1)} — Language for generated comments and messages:
\code{"pt"} (default), \code{"en"}, or \code{"es"}.}
}
\value{
Invisibly, the absolute path of the created project directory.
}
\description{
Creates a ready-to-use project directory with a \verb{_targets.R} pipeline,
a parameterised Quarto report, optional \code{renv} dependency snapshot,
optional GitHub Actions CI workflow, and a structured file layout
following RAP best practices.
}
\section{Generated structure}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{<name>/
|-- _targets.R          # targets pipeline
|-- run.R               # convenience runner: source + tar_make()
|-- analysis.qmd        # parameterised Quarto report
|-- R/
|   `-- functions.R     # custom analysis functions
|-- data/               # raw data (git-ignored)
|-- output/             # results (git-ignored)
|-- renv/               # renv library (when include_renv = TRUE)
|-- renv.lock
|-- .gitignore
`-- README.md
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
sus_rap_template(
  path   = "~/projetos",
  name   = "dengue_am",
  system = "SINAN",
  lang   = "pt"
)
}
}
