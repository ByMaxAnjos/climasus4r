% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-S3.R
\name{write_duckdb_climasus}
\alias{write_duckdb_climasus}
\title{Write a climasus_df to a persistent DuckDB file}
\usage{
write_duckdb_climasus(x, path, view_name = "climasus_data", overwrite = TRUE)
}
\arguments{
\item{x}{A \code{climasus_df} object.}

\item{path}{Character. File path for the DuckDB database (e.g.
\code{"data/sim.duckdb"}).}

\item{view_name}{Character. Name of the table inside the DuckDB file
(default: \code{"climasus_data"}).}

\item{overwrite}{Logical. Whether to overwrite an existing table (default:
\code{TRUE}).}
}
\value{
\code{invisible(x)} — the original \code{climasus_df}, updated
with \code{backend = "duckdb"} in its metadata.
}
\description{
Convenience wrapper that opens a persistent DuckDB connection to
\code{path}, registers the data and metadata, then closes the connection.
}
\examples{
\dontrun{
df <- write_duckdb_climasus(df, "data/sim_respiratory.duckdb")
sus_meta(df, "backend")  # "duckdb"

# Read back later
con <- duckdb::dbConnect(duckdb::duckdb(), "data/sim_respiratory.duckdb")
df2 <- from_duckdb_climasus(con, "climasus_data")
duckdb::dbDisconnect(con)
}
}
