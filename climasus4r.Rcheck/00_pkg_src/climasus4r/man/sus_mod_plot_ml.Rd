% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_plot_ml.R
\name{sus_mod_plot_ml}
\alias{sus_mod_plot_ml}
\title{Plots and Tables from an XGBoost Machine Learning Model}
\usage{
sus_mod_plot_ml(
  x,
  type = c("importance", "fit", "cv_log"),
  output_type = c("plot", "table", "all"),
  n_top = 20L,
  interactive = FALSE,
  base_size = 12L,
  save_plot = NULL,
  lang = c("pt", "en", "es"),
  verbose = FALSE
)
}
\arguments{
\item{x}{A \code{climasus_ml} object produced by \code{\link[=sus_mod_ml]{sus_mod_ml()}}.}

\item{type}{Character. Plot type: \code{"importance"} (default), \code{"fit"}, or
\code{"cv_log"}.}

\item{output_type}{Character. \code{"plot"} (default), \code{"table"}, or \code{"all"}
(named list \verb{$plot}, \verb{$table}, \verb{$data}).}

\item{n_top}{Integer. Maximum number of features to show in the importance
plot. Default \code{20L}.}

\item{interactive}{Logical. \code{TRUE} converts the ggplot2 output to an
interactive \pkg{plotly} widget. Default \code{FALSE}.}

\item{base_size}{Numeric. ggplot2 base font size. Default \code{12}.}

\item{save_plot}{Character or \code{NULL}. File path to save the plot.}

\item{lang}{Character. Language for labels: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{FALSE}.}
}
\value{
Depending on \code{output_type}:
\itemize{
\item \code{"plot"} -> a \code{ggplot} or \code{plotly} object.
\item \code{"table"} -> a \code{tibble} of the plotted data.
\item \code{"all"} -> a named list: \verb{$plot}, \verb{$table}, \verb{$data}.
}
}
\description{
Produces feature importance charts, observed-vs-predicted scatter plots,
and cross-validation loss curves from a \code{climasus_ml} object returned by
\code{\link[=sus_mod_ml]{sus_mod_ml()}}.
}
\section{Plot types (\code{type})}{
\tabular{ll}{
   \code{type} \tab Description \cr
   \code{"importance"} \tab Horizontal bar chart of XGBoost feature Gain (top \code{n_top} features) \cr
   \code{"fit"} \tab Observed vs. out-of-fold CV-predicted scatter with R² and RMSE annotation \cr
   \code{"cv_log"} \tab Train/test loss per boosting round from \code{xgb.cv()}, with best-round marker \cr
}
}

\examples{
\dontrun{
ml <- sus_mod_ml(df, outcome_col = "n_obitos", feature_cols = c("tmax", "pop"))

sus_mod_plot_ml(ml, type = "importance", lang = "pt")
sus_mod_plot_ml(ml, type = "fit",        lang = "en")
sus_mod_plot_ml(ml, type = "cv_log",     lang = "es")
out <- sus_mod_plot_ml(ml, output_type = "all")
out$table
}

}
\seealso{
\code{\link[=sus_mod_ml]{sus_mod_ml()}}, \code{\link[=sus_mod_plot_dlnm]{sus_mod_plot_dlnm()}}
}
