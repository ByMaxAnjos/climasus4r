% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_plot_spatial_moran.R
\name{sus_mod_plot_spatial_moran}
\alias{sus_mod_plot_spatial_moran}
\title{Two-Panel LISA Visualisation: Cluster Map and Moran Scatter Plot}
\usage{
sus_mod_plot_spatial_moran(
  x,
  municipalities = NULL,
  type = c("map", "scatter", "both"),
  alpha = 0.05,
  title = NULL,
  lang = "pt",
  ...
)
}
\arguments{
\item{x}{A \code{climasus_spatial_moran} object from \code{\link[=sus_mod_spatial_moran]{sus_mod_spatial_moran()}}.}

\item{municipalities}{An \code{sf} object with at least the column \code{code_muni}
(7-digit IBGE code) and polygon geometry. Required when \code{type} is \code{"map"}
or \code{"both"}. Ignored for \code{type = "scatter"}.}

\item{type}{Character. Which panel(s) to produce. One of \code{"map"} (default),
\code{"scatter"}, or \code{"both"}.}

\item{alpha}{Numeric in (0, 1). Significance level used for the subtitle
annotation. Default \code{0.05}.}

\item{title}{Character or \code{NULL}. Custom title overriding the default
translated title. Applies to the first / only panel. Default \code{NULL}.}

\item{lang}{Character. Output language: \code{"pt"} (default), \code{"en"}, or \code{"es"}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} when saving,
or ignored.}
}
\value{
\itemize{
\item \code{type = "map"} -> a \code{ggplot} object.
\item \code{type = "scatter"} -> a \code{ggplot} object.
\item \code{type = "both"} -> a \code{patchwork} / \code{ggplot} combined object, or a named
list \verb{list(map = <ggplot>, scatter = <ggplot>)} if \pkg{patchwork} is not
installed.
}
}
\description{
Produces a choropleth LISA cluster map and/or a Moran scatter plot from a
\code{climasus_spatial_moran} object returned by \code{\link[=sus_mod_spatial_moran]{sus_mod_spatial_moran()}}.
}
\section{Panel types (\code{type})}{
\tabular{ll}{
   \code{type} \tab Description \cr
   \code{"map"} \tab Choropleth map coloured by LISA quadrant (HH/LL/HL/LH/NS). \cr
   \code{"scatter"} \tab Moran scatter plot of standardised values vs. spatial lag. \cr
   \code{"both"} \tab Side-by-side panels via \pkg{patchwork} (named list fallback if not installed). \cr
}
}

\section{Colour scheme}{

Quadrant colours follow the conventional LISA palette: HH = \code{"red3"},
LL = \code{"blue3"}, HL = \code{"darkorange"}, LH = \code{"steelblue"}, NS = \code{"grey80"}.
}

\examples{
\dontrun{
# Requires spdep, sf, ggplot2 (and optionally patchwork)
library(climasus4r)

# moran_res <- sus_mod_spatial_moran(df, "deaths", W)
# shp <- geobr::read_municipality(code_muni = "all", year = 2020)

sus_mod_plot_spatial_moran(moran_res, municipalities = shp, type = "both")
sus_mod_plot_spatial_moran(moran_res, municipalities = shp, type = "map", lang = "en")
sus_mod_plot_spatial_moran(moran_res, type = "scatter", lang = "es")
}

}
\seealso{
\code{\link[=sus_mod_spatial_moran]{sus_mod_spatial_moran()}}, \code{\link[=sus_mod_spatial_weights]{sus_mod_spatial_weights()}}
}
