% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_its.R
\name{sus_mod_its}
\alias{sus_mod_its}
\title{Interrupted Time Series Analysis for Health Outcome Counts}
\usage{
sus_mod_its(
  data,
  outcome_col = "n_obitos",
  date_col = "date",
  interruption_dates,
  harmonics = 2L,
  family = "quasipoisson",
  covariates = NULL,
  alpha = 0.05,
  counterfactual = TRUE,
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{climasus_df} at stage \code{"aggregate"} or \code{"climate"}, or any
\code{data.frame} with a date column and an integer count column.}

\item{outcome_col}{Character. Name of the daily health outcome count column.
Default: \code{"n_obitos"}.}

\item{date_col}{Character. Name of the date column. Default: \code{"date"}.}

\item{interruption_dates}{Date or character vector of at least one
interruption date (ISO \code{"YYYY-MM-DD"} format). Must lie strictly within
the data range. For a single interruption pass a scalar;
for multiple, pass a vector.}

\item{harmonics}{Integer. Number of sinusoidal harmonic pairs for seasonal
control (\eqn{H} in the model above). \code{0} suppresses seasonal terms.
Default: \code{2L}.}

\item{family}{Character. GLM family: \code{"quasipoisson"} (default, robust to
overdispersion) or \code{"poisson"}.}

\item{covariates}{Character vector or \code{NULL}. Names of additional columns to
include as linear confounders (e.g., \code{"rh_pct"}, \code{"holiday"}). Must be
numeric or dummy-coded. Default: \code{NULL}.}

\item{alpha}{Numeric. Significance level for confidence intervals.
Default: \code{0.05} (95\% CI).}

\item{counterfactual}{Logical. Compute and return counterfactual predictions.
Default: \code{TRUE}.}

\item{lang}{Character. Language for messages: \code{"pt"} (default), \code{"en"},
\code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{climasus_its} list with:
\describe{
\item{\verb{$model}}{The fitted \code{glm} object.}
\item{\verb{$effects}}{Tibble with one row per interruption: \code{label},
\code{interruption_date}, \code{level_ratio}, \code{level_ci_lo}, \code{level_ci_hi},
\code{level_p}, \code{slope_daily_log}, \code{slope_ratio_annual}, \code{slope_p}.}
\item{\verb{$counterfactual}}{Tibble with daily \code{date}, \code{observed},
\code{predicted} (fitted values), \code{counterfactual}, \code{cf_lo}, \code{cf_hi},
\code{ratio_to_cf}, \code{prevented}. \code{NULL} when \code{counterfactual = FALSE}.}
\item{\verb{$segments}}{Tibble with one row per segment (pre-interruption and
each post-interruption interval): \code{segment}, \code{start_date}, \code{end_date},
\code{n_days}, \code{mean_observed}, \code{mean_predicted}, \code{mean_counterfactual}.}
\item{\verb{$data}}{The analysis dataset with all model covariates.}
\item{\verb{$meta}}{Analysis parameters and diagnostics.}
}
}
\description{
Fits a segmented quasi-Poisson regression to estimate the immediate and
sustained effects of one or more interruptions (policies, extreme events,
pandemics) on a daily health outcome count series. The model decomposes
each interruption into a \strong{level change} (immediate step) and a \strong{slope
change} (sustained trend shift), controlling for the underlying time trend
and seasonal variation via harmonic terms. A counterfactual projection shows
what would have been expected had the interruption not occurred.
}
\section{Statistical framework}{


For \eqn{K} interruption dates \eqn{T_1 < T_2 < \ldots < T_K}, the
segmented Poisson GLM is:

\deqn{\log(E[Y_t]) = \beta_0 + \beta_1 t
  + \sum_{j=1}^{K}\left[\beta_{2j}\, D_j(t)
                       + \beta_{2j+1}\, S_j(t)\right]
  + \sum_{k=1}^{H}\left[\gamma_k \sin\!\left(\tfrac{2\pi k\, t}{365.25}\right)
                       + \delta_k \cos\!\left(\tfrac{2\pi k\, t}{365.25}\right)\right]
  + \mathbf{z}_t^\top \boldsymbol{\eta}}

where \eqn{D_j(t) = \mathbf{1}(t \ge T_j)} is the step indicator and
\eqn{S_j(t) = \max(0,\, t - T_j)} is the slope-change ("hockey stick")
basis. The immediate rate ratio at interruption \eqn{j} is
\eqn{RR_j = e^{\beta_{2j}}}; the sustained trend shift is
\eqn{e^{\beta_{2j+1}}} per day (or \eqn{e^{365.25\,\beta_{2j+1}}} per year).
}

\section{Counterfactual}{


The counterfactual is the model prediction with all \eqn{D_j} and \eqn{S_j}
set to zero — i.e., the pre-interruption baseline trend projected forward
through the study period. The difference \code{observed - counterfactual} is the
estimated total attributable change.
}

\examples{
\dontrun{
# Single interruption (COVID-19 pandemic onset)
its <- sus_mod_its(
  df_daily,
  outcome_col       = "n_obitos",
  interruption_dates = "2020-03-17",
  harmonics         = 2L,
  lang              = "pt"
)
print(its)
tidy(its)

# Multiple interruptions
its2 <- sus_mod_its(
  df_daily,
  outcome_col        = "n_obitos",
  interruption_dates = c("2020-03-17", "2021-01-18"),
  lang               = "en"
)
}

}
\references{
Bernal, J.L., Cummins, S., & Gasparrini, A. (2017). Interrupted time
series regression for the evaluation of public health interventions: a
tutorial. \emph{International Journal of Epidemiology}, 46(1), 348-355.
\doi{10.1093/ije/dyw098}

Penfold, R.B., & Zhang, F. (2013). Use of interrupted time series analysis
in evaluating health care quality improvements. \emph{Academic Pediatrics},
13(6 Suppl), S38-44. \doi{10.1016/j.acap.2013.08.002}
}
\seealso{
\code{\link[=sus_mod_excess]{sus_mod_excess()}}, \code{\link[=sus_mod_casecrossover]{sus_mod_casecrossover()}}, \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}
}
