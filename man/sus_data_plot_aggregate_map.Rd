% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_data_plot_aggregate_map.R
\name{sus_data_plot_aggregate_map}
\alias{sus_data_plot_aggregate_map}
\title{Plot Municipal Map of Aggregated Health Data}
\usage{
sus_data_plot_aggregate_map(
  df,
  value_col = NULL,
  map_type = c("bubble", "choropleth"),
  rate_per_100k = FALSE,
  period = NULL,
  top_n = NULL,
  state_borders = TRUE,
  show_labels = TRUE,
  palette = "YlOrRd",
  log_scale = TRUE,
  title = NULL,
  base_size = 11,
  interactive = FALSE,
  use_cache = TRUE,
  cache_dir = "~/.climasus4r_cache/spatial",
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{df}{A \code{climasus_df} at \code{stage = "aggregate"} or later (output of
\code{\link[=sus_data_aggregate]{sus_data_aggregate()}}).}

\item{value_col}{Character.  Name of the health-count column to map.
If \code{NULL} (default) the column is auto-detected using the same priority
order as \code{\link[=sus_climate_plot_aggregate]{sus_climate_plot_aggregate()}}:
\code{n_obitos}, \code{n_internacoes}, \code{n_nascimentos}, \code{n_casos},
\code{n_procedimentos}, \code{n_estabelecimentos} and their English / Spanish
equivalents.}

\item{map_type}{Character.  \code{"bubble"} (default) or \code{"choropleth"}.}

\item{rate_per_100k}{Logical.  If \code{TRUE}, divides \code{total_cases} by \code{pop_25}
(from \code{municipio_meta}) and multiplies by 1e5 to compute incidence per
100,000 inhabitants.  Default \code{FALSE}.}

\item{period}{Date scalar or \code{c(start, end)} vector.  When supplied, rows
in \code{df} whose \code{date} column falls outside the interval are removed before
aggregation.  \code{NULL} (default) uses all rows.}

\item{top_n}{Integer.  If supplied, only the top N municipalities by
\code{total_cases} are labelled on the map.  \code{NULL} disables top-N labels.}

\item{state_borders}{Logical.  Overlay state boundary polygons
(downloaded via \code{geobr}).  Default \code{TRUE}.}

\item{show_labels}{Logical.  Annotate capital cities with text labels
(bubble map only).  Default \code{TRUE}.}

\item{palette}{Character.  Colour palette for the fill / bubble fill scale.
Any \code{\link[RColorBrewer:ColorBrewer]{RColorBrewer::brewer.pal()}} sequential/diverging palette name
(e.g. \code{"YlOrRd"}, \code{"Blues"}, \code{"RdYlBu"}) or one of the \code{ggsci} aliases
\code{"lancet"} / \code{"nejm"}.  Default \code{"YlOrRd"}.}

\item{log_scale}{Logical.  Apply \code{log1p} transformation to the colour
scale.  Default \code{TRUE}.}

\item{title}{Character.  Map title.  \code{NULL} uses a built-in multilingual
default.}

\item{base_size}{Numeric.  Base font size for \code{theme_void()}.  Default \code{11}.}

\item{interactive}{Logical.  If \code{TRUE}, wraps the ggplot2 object with
\code{plotly::ggplotly()}.  Requires \code{plotly}.  Default \code{FALSE}.}

\item{use_cache}{Logical.  Cache municipality metadata to disk.
Default \code{TRUE}.}

\item{cache_dir}{Character.  Directory for the disk cache.
Default \code{"~/.climasus4r_cache/spatial"}.}

\item{lang}{Character.  Language for messages and axis labels:
\code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical.  Print progress messages.  Default \code{TRUE}.}
}
\value{
A \code{ggplot2} object (class \code{"gg"} / \code{"ggplot"}), or a \code{plotly}
object when \code{interactive = TRUE}.  The function does \strong{not} modify \code{df}
or advance the pipeline \code{stage}.
}
\description{
Renders a bubble or choropleth map showing the spatial distribution of
health events (counts or incidence rates) at the Brazilian municipal level.
Designed for data produced by \code{\link[=sus_data_aggregate]{sus_data_aggregate()}}.

\strong{Bubble map} (\code{map_type = "bubble"}) places proportionally sized,
colour-encoded circles at each municipality centroid.  State boundaries are
drawn as a background polygon layer.

\strong{Choropleth map} (\code{map_type = "choropleth"}) fills municipality polygons
downloaded via \code{\link[geobr:read_municipality]{geobr::read_municipality()}}.  Requires \code{geobr} and \code{sf}.
If these packages are unavailable the function falls back to a bubble map
with a warning.
}
\examples{
\dontrun{
library(climasus4r)

# Monthly deaths by municipality
df_agg <- sus_data_import(uf = "RN", year = 2022, system = "SIM-DO") |>
  sus_data_standardize() |>
  sus_data_aggregate(time_unit = "month",
                     group_by  = "codigo_municipio_residencia")

# Default bubble map (Portuguese labels)
sus_data_plot_aggregate_map(df_agg, lang = "pt")

# Choropleth with incidence rate per 100k, English labels
sus_data_plot_aggregate_map(
  df_agg,
  map_type      = "choropleth",
  rate_per_100k = TRUE,
  palette       = "Blues",
  lang          = "en"
)

# Interactive plotly bubble map
sus_data_plot_aggregate_map(df_agg, interactive = TRUE, lang = "pt")
}

}
\seealso{
\code{\link[=sus_data_aggregate]{sus_data_aggregate()}}, \code{\link[=sus_climate_plot_aggregate]{sus_climate_plot_aggregate()}}
}
