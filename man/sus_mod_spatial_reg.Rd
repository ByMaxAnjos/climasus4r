% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_spatial_reg.R
\name{sus_mod_spatial_reg}
\alias{sus_mod_spatial_reg}
\title{Spatial Regression (SAR / SEM / SDM / SAC) for Climate-Health Associations}
\usage{
sus_mod_spatial_reg(
  df,
  formula,
  W,
  model = c("lag", "error", "durbin", "sac"),
  method = c("eigen", "LU", "Chebyshev", "MC"),
  zero_policy = TRUE,
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{df}{A \code{data.frame} or \code{climasus_df} containing all variables in
\code{formula}. If an \code{sf} object, geometry is dropped automatically.}

\item{formula}{A \code{formula} specifying the outcome and predictors, e.g.
\code{deaths ~ mean_temp + precip + pib_pc}.}

\item{W}{A \code{climasus_weights} object produced by
\code{\link[=sus_mod_spatial_weights]{sus_mod_spatial_weights()}}. Must contain a \code{listw} element whose
length matches \code{nrow(df)}.}

\item{model}{Character. Spatial model family. One of \code{"lag"} (SAR,
default), \code{"error"} (SEM), \code{"durbin"} (SDM), or \code{"sac"} (SAC).}

\item{method}{Character. Estimation method passed to the underlying
\strong{spatialreg} function. One of \code{"eigen"} (default), \code{"LU"},
\code{"Chebyshev"}, or \code{"MC"}. \code{"eigen"} is exact; the others are
approximations suitable for large datasets.}

\item{zero_policy}{Logical. If \code{TRUE} (default), spatial units with no
neighbours (islands) are allowed and receive a zero spatial lag/error.
Passed to all \strong{spatialreg} and \strong{spdep} calls.}

\item{lang}{Character. Output language for messages: \code{"pt"} (default),
\code{"en"}, or \code{"es"}.}

\item{verbose}{Logical. If \code{TRUE} (default), print progress messages via
\strong{cli}.}
}
\value{
An object of class \code{c("climasus_spatial_reg", "list")} with the
following named elements:
\describe{
\item{\verb{$model}}{Character. The model argument value.}
\item{\verb{$model_label}}{Character. Human-readable model name.}
\item{\verb{$coefficients}}{\code{data.frame} with columns \code{term}, \code{estimate},
\code{std_error}, \code{z_value}, \code{p_value} for all covariates (excluding
the spatial parameters \code{rho}/\code{lambda}).}
\item{\verb{$rho}}{Numeric spatial lag parameter (\code{NULL} for the \code{error}
model).}
\item{\verb{$lambda}}{Numeric spatial error parameter (\code{NULL} for the
\code{lag} and \code{durbin} models).}
\item{\verb{$impacts}}{\code{data.frame} with columns \code{term}, \code{direct},
\code{indirect}, \code{total} (from \code{spatialreg::impacts()}), or \code{NULL} for
the \code{error} model or when impact computation fails.}
\item{\verb{$aic}}{Numeric. AIC of the spatial model.}
\item{\verb{$lm_aic}}{Numeric. AIC of the OLS baseline (same formula,
same data).}
\item{\verb{$moran_residuals}}{Named list with elements \code{I} (Moran
statistic), \code{p_value} (analytical p-value), and \code{z} (z-score).}
\item{\verb{$fitted}}{Numeric vector of fitted values.}
\item{\verb{$residuals}}{Numeric vector of model residuals.}
\item{\verb{$call}}{The matched function call.}
}
}
\description{
Fits maximum-likelihood spatial regression models that account for spatial
spillovers between geographic units (e.g. municipalities) in climate-health
studies. Four model families are supported: the Spatial Autoregressive Model
(SAR / spatial lag), the Spatial Error Model (SEM), the Spatial Durbin Model
(SDM), and the Spatial Autocorrelation Model (SAC).
}
\section{Model families}{


\describe{
\item{\code{"lag"} (SAR)}{
The outcome in unit \eqn{i} depends on a weighted average of outcomes
in neighbouring units (spatial lag). Estimated via
\code{spatialreg::lagsarlm()}. Contains a spatial lag parameter \code{rho}.
}
\item{\code{"error"} (SEM)}{
Spatial dependence enters through a spatially autocorrelated error term.
Estimated via \code{spatialreg::errorsarlm()}. Contains a spatial error
parameter \code{lambda}.
}
\item{\code{"durbin"} (SDM)}{
Extends the SAR model by including spatially lagged covariates (Durbin
terms). Estimated via \code{spatialreg::lagsarlm()} with \code{Durbin = TRUE}.
Contains both \code{rho} and lagged-covariate coefficients.
}
\item{\code{"sac"} (SAC)}{
Contains both a spatial lag of the outcome (\code{rho}) and a spatial error
term (\code{lambda}). Estimated via \code{spatialreg::sacsarlm()}.
}
}
}

\section{Impacts}{


For models with a spatial lag (\code{lag}, \code{durbin}, \code{sac}), the total effect of
a covariate is decomposed into \emph{direct} (own-unit), \emph{indirect} (spillover),
and \emph{total} effects using \code{spatialreg::impacts()} with \code{R} simulations for
inference. For the \code{error} model impacts equal OLS slopes and no
decomposition is meaningful; the \verb{$impacts} element will be \code{NULL}.
}

\section{Residual diagnostics}{


After fitting, \code{\link[spdep:moran.test]{spdep::moran.test()}} is applied to the model residuals to
verify that spatial autocorrelation has been adequately absorbed. A
non-significant Moran p-value indicates a well-specified spatial model.
}

\examples{
\dontrun{
library(climasus4r)

# W <- sus_mod_spatial_weights(shp, style = "W")
result <- sus_mod_spatial_reg(
  df      = my_df,
  formula = deaths ~ mean_temp + precip + pib_pc,
  W       = W,
  model   = "lag",
  lang    = "pt"
)
print(result)
summary(result)

# SDM with spillovers
result_sdm <- sus_mod_spatial_reg(
  df      = my_df,
  formula = deaths ~ mean_temp + precip,
  W       = W,
  model   = "durbin"
)
result_sdm$impacts
}

}
\references{
Anselin, L. (1988). \emph{Spatial Econometrics: Methods and Models}.
Kluwer Academic Publishers.

LeSage, J., & Pace, R. K. (2009). \emph{Introduction to Spatial
Econometrics}. CRC Press / Taylor & Francis.

Bivand, R. S., Pebesma, E., & Gomez-Rubio, V. (2013).
\emph{Applied Spatial Data Analysis with R} (2nd ed.). Springer.

Elhorst, J. P. (2014). \emph{Spatial Econometrics: From Cross-Sectional
Data to Panels}. Springer.
}
\seealso{
\code{\link[=sus_mod_spatial_weights]{sus_mod_spatial_weights()}}, \code{\link[=sus_mod_spatial_moran]{sus_mod_spatial_moran()}},
\code{\link[=sus_mod_spatial_scan]{sus_mod_spatial_scan()}}
}
