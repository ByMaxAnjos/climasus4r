% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_af.R
\name{sus_mod_af}
\alias{sus_mod_af}
\title{Attributable Fraction and Number from a DLNM Fit}
\usage{
sus_mod_af(
  fit,
  threshold = NULL,
  range = NULL,
  pred_at = c(0.75, 0.9, 0.95, 0.99),
  by = NULL,
  nsim = 1000L,
  alpha = 0.05,
  lang = c("pt", "en", "es"),
  verbose = TRUE
)
}
\arguments{
\item{fit}{A \code{climasus_dlnm} object produced by \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}.}

\item{threshold}{Numeric or \code{NULL}. Exposure value splitting heat (above) and
cold (below) components. \code{NULL} (default) uses \code{fit$meta$ref_value}.}

\item{range}{Numeric vector \code{c(low, high)} or \code{NULL}. When provided, AF is
also computed for this custom exposure range.}

\item{pred_at}{Numeric vector (0-1). Quantiles defining the percentile-band
table. For each \code{q}, the AF above the \code{q}-th quantile (hot) and below the
\code{(1-q)}-th quantile (cold) is reported. Default \code{c(0.75, 0.90, 0.95, 0.99)}.}

\item{by}{Character or \code{NULL}. Temporal grouping for the period table:
\code{"month"}, \code{"year"}, or \code{"season"}. \code{NULL} (default) skips the breakdown.}

\item{nsim}{Integer. Number of Monte Carlo simulations for CI. Default
\code{1000L}. Reduce to \code{200L} for quick exploratory runs; increase to \code{5000L}
for publication-quality CI.}

\item{alpha}{Numeric. Significance level for CI. Default \code{0.05} (95\%).}

\item{lang}{Character. Language for messages: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{climasus_af} object (named list) with:
\describe{
\item{\verb{$total}}{One-row-per-component tibble (total/heat/cold) with AF,
AN, and 95\% CI.}
\item{\verb{$by_quantile}}{AF/AN at each percentile band in \code{pred_at}.}
\item{\verb{$by_period}}{Monthly/yearly/seasonal point estimates; \code{NULL} when
\code{by = NULL}.}
\item{\verb{$daily}}{Per-day date + exposure + cases + AN + AF for mapping.}
\item{\verb{$custom}}{AF/AN for the custom \code{range}; \code{NULL} when not provided.}
\item{\verb{$meta}}{Named list of all parameters used.}
}
}
\description{
Computes population attributable fractions (AF) and attributable numbers (AN)
from a \code{climasus_dlnm} object returned by \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}. Decomposes the
total burden into heat and cold components, reports AF at user-defined
exposure percentile ranges, and optionally breaks the burden down by month,
year, or season.
}
\section{Methodology}{

Point estimates are obtained by calling \code{dlnm::crosspred()} at the observed
exposure values and computing \eqn{AN_t = n_t \times (1 - 1/RR_t)} for each
day. Confidence intervals use Monte Carlo simulation: \code{nsim} coefficient
vectors are drawn from \eqn{N(\hat{\beta}_{cb}, \widehat{V}_{cb})} via
\code{MASS::mvrnorm}, crosspred is re-evaluated for each draw, and AF quantiles
yield the CI. When \pkg{MASS} is unavailable the function falls back to
delta-method bounds from the original crosspred object.
}

\examples{
\dontrun{
fit <- sus_mod_dlnm(df_dl, outcome_col = "n_obitos", lang = "pt")

# Total burden with heat/cold split
af <- sus_mod_af(fit, lang = "pt")
print(af)
tidy(af)

# Monthly temporal breakdown
af_m <- sus_mod_af(fit, by = "month", nsim = 200L, lang = "en")
af_m$by_period

# Pool across multiple cities
dplyr::bind_rows(tidy(af_city1), tidy(af_city2))
}

}
\references{
Gasparrini, A., & Armstrong, B. (2013). The impact of heat waves on mortality.
\emph{Epidemiology}, 24(1), 64-71.
\doi{10.1097/EDE.0b013e3182770cb4}

Gasparrini, A., \emph{et al.} (2017). Attributable causes of heat-related mortality
across 739 locations. \emph{The Lancet Planetary Health}, 1(9), e360-e367.
\doi{10.1016/S2542-5196(17)30156-0}
}
\seealso{
\code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}, \code{\link[=sus_mod_plot_dlnm]{sus_mod_plot_dlnm()}}
}
