% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_pool.R
\name{sus_mod_pool}
\alias{sus_mod_pool}
\title{Two-Stage Multi-City Pooling of DLNM Estimates}
\usage{
sus_mod_pool(
  fits,
  exposure_range = NULL,
  n_grid = 100L,
  pred_at = c(0.75, 0.9, 0.95, 0.99),
  blup = TRUE,
  method = "reml",
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{fits}{A named list of \code{climasus_dlnm} objects, one per city or region.
All fits must have been produced with the same \code{climate_col}, \code{argvar},
\code{arglag}, and \code{lag_max}.}

\item{exposure_range}{Numeric vector of length 2 specifying the exposure
grid range for the pooled prediction. Default \code{NULL} uses the combined
range across all city datasets.}

\item{n_grid}{Integer. Number of exposure grid points for the pooled
prediction curve. Default \code{100L}.}

\item{pred_at}{Numeric vector of quantile probabilities (0-1) for the
pooled exposure-response summary table. Default \code{c(0.75, 0.90, 0.95, 0.99)}.}

\item{blup}{Logical. Compute BLUP city-specific predictions? Default \code{TRUE}.}

\item{method}{Character. \code{mvmeta} estimation method: \code{"reml"} (default,
recommended), \code{"ml"}, or \code{"fixed"} (fixed-effects, no heterogeneity).}

\item{lang}{Character. Language for messages: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{climasus_pool} list with components:
\describe{
\item{\code{mvmeta_fit}}{The raw \code{mvmeta} model object.}
\item{\code{pooled_pred}}{\code{dlnm::crosspred} object with pooled coefficients.}
\item{\code{exposure_response}}{Tibble of pooled RR at quantile grid points.}
\item{\code{lag_response}}{Tibble of pooled RR by lag at the 75th percentile exposure.}
\item{\code{blup_preds}}{Named list of city-specific BLUP \code{crosspred} objects
(when \code{blup = TRUE}).}
\item{\code{city_table}}{Tibble with one row per city showing city-specific
and BLUP-adjusted RRs at the 75th percentile.}
\item{\code{heterogeneity}}{Tibble with Cochran Q, df, p-value, and I^2
statistics.}
\item{\code{meta}}{List of metadata: \code{climate_col}, \code{outcome_col}, \code{lag_max},
\code{n_cities}, \code{city_names}, \code{method}, \code{argvar}, \code{arglag}, \code{ref_value},
\code{pred_at}, \code{call_time}.}
}
}
\description{
Performs a two-stage multivariate meta-analysis to pool Distributed Lag
Non-linear Model (DLNM) estimates across multiple cities or regions.
Stage 1 city-specific estimates (coefficients and variance-covariance matrices
from \code{sus_mod_dlnm()}) are combined using multivariate random-effects
meta-analysis via \code{mvmeta}, producing pooled exposure-response and
lag-response curves, heterogeneity statistics, and Best Linear Unbiased
Predictors (BLUPs) for each city.
}
\section{Statistical framework}{


Let \eqn{\boldsymbol{\theta}_i} be the \eqn{p}-dimensional vector of
cross-basis coefficients for city \eqn{i}, estimated with variance
\eqn{S_i}. The multivariate random-effects model is:

\deqn{\boldsymbol{\theta}_i \sim N(\boldsymbol{\theta}, S_i + \Psi)}

where \eqn{\Psi} is the between-city covariance matrix estimated by
REML. The pooled estimate \eqn{\hat{\boldsymbol{\theta}}} and its
variance are passed to \code{dlnm::crosspred()} to produce a pooled
exposure-response surface identical in format to the single-city output.

BLUPs shrink city-specific estimates toward the pooled mean:

\deqn{\tilde{\boldsymbol{\theta}}_i = \hat{\boldsymbol{\theta}} +
  \hat\Psi (\hat\Psi + S_i)^{-1}
  (\hat{\boldsymbol{\theta}}_i - \hat{\boldsymbol{\theta}})}
}

\examples{
\dontrun{
fits <- list(
  city_a = sus_mod_dlnm(df_a, ...),
  city_b = sus_mod_dlnm(df_b, ...),
  city_c = sus_mod_dlnm(df_c, ...)
)
pool <- sus_mod_pool(fits, lang = "en")
print(pool)
plot_pool <- sus_mod_plot_dlnm(pool, lang = "en")
}

}
