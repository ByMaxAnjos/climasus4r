% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_climate_uniplu.R
\name{sus_climate_uniplu}
\alias{sus_climate_uniplu}
\title{Import UNIPLU-BR: Unified Brazilian Rainfall Dataset}
\usage{
sus_climate_uniplu(
  years = NULL,
  uf = NULL,
  network = NULL,
  aggregate_to = "day",
  use_cache = TRUE,
  cache_dir = "~/.climasus4r_cache/climate",
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{years}{Integer vector of year(s) to import.
Examples: \code{2020}, \code{2020:2024}, \code{c(2015, 2020, 2024)}.
Must be between 1885 and the current year. If \code{NULL} (default), imports
the last 2 years.}

\item{uf}{Character vector of Brazilian state codes
(e.g., \code{"RN"}, \code{c("SP", "RJ", "MG")}). Case insensitive. If \code{NULL}
(default), all 27 states are returned.}

\item{network}{Character vector of monitoring network(s) to include.
One or more of: \code{"Hidroweb"}, \code{"INMET"}, \code{"ICEA"}, \code{"CEMADEN"},
\code{"Telemetria"}. Case insensitive. If \code{NULL} (default), all networks
are returned.}

\item{aggregate_to}{Character. Temporal resolution for aggregating sub-daily
observations into totals. One of:
\itemize{
\item \code{"day"} (default): Daily rainfall totals (\code{sum(rain_mm)})
\item \code{"month"}: Monthly rainfall totals
\item \code{"year"}: Annual rainfall totals
\item \code{"none"}: Return raw observations at original resolution
}
When \code{"none"}, \code{rainfall_mm} reflects the raw measurement interval
(10 min to 1440 min depending on network). Use \code{"day"} for compatibility
with \code{sus_climate_aggregate()}.}

\item{use_cache}{Logical. If \code{TRUE} (default), skips download when the two
Parquet files already exist in \verb{cache_dir/uniplu/}. The full ZIP (~1.6 GB)
is only downloaded once. Use \code{use_cache = FALSE} to force a re-download
(e.g., after a new dataset version is published on Zenodo).}

\item{cache_dir}{Character. Directory path for the disk cache.
Default: \code{"~/.climasus4r_cache/climate"}. Created automatically if absent.
Use \code{unlink(file.path(cache_dir, "uniplu"), recursive = TRUE)} to clear
the UNIPLU cache.}

\item{lang}{Character. Message language. One of:
\itemize{
\item \code{"pt"}: Portuguese (default)
\item \code{"en"}: English
\item \code{"es"}: Spanish
}}

\item{verbose}{Logical. If \code{TRUE} (default), prints progress messages
including cache status, download progress, filtering counts, and
aggregation summary.}
}
\value{
A \code{climasus_df} object (subclass of \code{tibble}) at \code{stage = "climate"},
\code{type = "uniplu"}. Columns:

\describe{
\item{\code{station_code}}{Character. Unique gauge identifier (from \code{gauge_code})}
\item{\code{station_name}}{Character. City name of the gauge (from \code{city})}
\item{\code{uf}}{Character. State abbreviation (from \code{state})}
\item{\code{latitude}}{Numeric. Decimal latitude, WGS84 (from \code{lat})}
\item{\code{longitude}}{Numeric. Decimal longitude, WGS84 (from \code{long})}
\item{\code{altitude}}{Numeric. Elevation in metres above sea level (from \code{elevation})}
\item{\code{network}}{Character. Source monitoring network}
\item{\code{time_step_min}}{Integer. Original temporal resolution in minutes
(e.g., 10, 60, 1440). Only present when \code{aggregate_to = "none"}.}
\item{\code{utc_offset}}{Numeric. Timezone offset from UTC (from \code{utc}).
Only present when \code{aggregate_to = "none"}.}
\item{\code{date}}{POSIXct or Date. Observation timestamp / aggregation period}
\item{\code{rainfall_mm}}{Numeric. Precipitation in millimetres
(sum over aggregation period, or raw value when \code{aggregate_to = "none"})}
}

\strong{Metadata} (accessible via \code{sus_meta()}):
\describe{
\item{\code{stage}}{"climate"}
\item{\code{type}}{"uniplu"}
\item{\code{source}}{"UNIPLU-BR"}
\item{\code{doi}}{"10.5281/zenodo.18883358"}
\item{\code{years}}{Years retained after filtering}
\item{\code{ufs}}{States retained after filtering}
\item{\code{networks}}{Networks retained after filtering}
\item{\code{n_stations}}{Number of unique gauges}
\item{\code{n_observations}}{Total rows}
\item{\code{temporal}}{List with \code{start}, \code{end}, \code{unit} (aggregate_to)}
}
}
\description{
\code{sus_climate_uniplu()} downloads, caches, and imports data from the
\strong{Unified Brazilian Rainfall Dataset (UNIPLU-BR)} — the most comprehensive
national rainfall database for Brazil, covering \strong{21,000+ gauges} from
five monitoring networks over \strong{140 years (1885–2025)}.

The function implements a full processing pipeline:
\enumerate{
\item \strong{Download}: Single 1.6 GB ZIP from Zenodo (first call only)
\item \strong{Extraction}: Two Parquet files cached locally for instant re-use
\item \strong{Standardization}: Column names aligned with climasus4r conventions
\item \strong{Filtering}: By year, state (UF), and/or monitoring network
\item \strong{Aggregation}: Sub-daily data optionally aggregated to daily/monthly/yearly totals
}
}
\note{
\itemize{
\item \strong{Data quality}: The dataset is strictly structurally standardized.
\strong{No outlier removal or physical consistency checks were applied} by the
data producers. Extreme or implausible values from source agencies remain.
Consider applying your own quality filters for scientific analyses.
\item \strong{Download size}: ~1.6 GB compressed. Only performed once; subsequent
calls read from cached Parquet files (typically a few seconds).
\item \strong{Rainfall only}: UNIPLU-BR contains precipitation exclusively.
For multi-variable climate data (temperature, humidity, radiation), use
\code{\link[=sus_climate_inmet]{sus_climate_inmet()}}.
\item \strong{License}: CC-BY-4.0. Cite the dataset and paper when publishing.
}
}
\section{Monitoring Networks}{
\tabular{llll}{
   \strong{Network} \tab \strong{Operator} \tab \strong{Period} \tab \strong{Resolution} \cr
   Hidroweb \tab ANA \tab 1885–2025 \tab Daily (1440 min) \cr
   INMET daily \tab INMET \tab 1889–2025 \tab Daily (1440 min) \cr
   ICEA \tab ICEA \tab 1951–2025 \tab Various \cr
   CEMADEN \tab CEMADEN \tab 2014–2025 \tab 10–15 min \cr
   Telemetria \tab Various \tab 2014–2025 \tab 10–60 min \cr
   INMET sub-daily \tab INMET \tab 2000–2025 \tab 60 min \cr
}
}

\examples{
\dontrun{
# Daily rainfall for Rio Grande do Norte, last 2 years (default)
rain_rn <- sus_climate_uniplu(uf = "RN")

# Multiple states, specific years, daily totals
rain_ne <- sus_climate_uniplu(
  years = 2015:2024,
  uf    = c("RN", "CE", "PB", "PE"),
  aggregate_to = "day"
)

# Only ANA/Hidroweb network, monthly totals
rain_hidroweb <- sus_climate_uniplu(
  years   = 2000:2020,
  network = "Hidroweb",
  aggregate_to = "month"
)

# Raw sub-daily data (no aggregation)
rain_raw <- sus_climate_uniplu(
  years        = 2023,
  uf           = "SP",
  network      = "CEMADEN",
  aggregate_to = "none"
)

# Check metadata
sus_meta(rain_rn)

# Inspect stations
dplyr::distinct(rain_rn, station_code, station_name, latitude, longitude)

# Integrate with health data (requires prior sus_spatial_join())
health_climate <- sus_climate_aggregate(
  health_data  = health_spatial,
  climate_data = rain_rn,
  climate_var  = "rainfall_mm",
  temporal_strategy = "moving_window",
  window_days  = 14
)
}

}
\references{
\strong{Dataset}:
Das Neves Almeida, C., Bertrand, G. F., Lemos, F. C., et al. (2026).
\emph{Unified Brazilian Rainfall Dataset (UNIPLU-BR): A Standardized National
Database of Point Precipitation from Major Brazilian Monitoring Networks
(1885–2025)}. Zenodo. \doi{10.5281/zenodo.18883358}

\strong{Related paper}:
Das Neves Almeida, C., Bertrand, G. F., Lemos, F. C., et al. (2025).
The design of the Brazilian Sub-Daily Rainfall dataset (BR-SDR): two decades
of high-time-resolution data in Brazil.
\emph{Hydrological Sciences Journal}, 70(11), 1850–1862.
\doi{10.1080/02626667.2025.2506193}

\strong{GitHub repository}: \url{https://github.com/LARHENA/UNIPLU-BR}
}
\seealso{
\itemize{
\item \code{\link[=sus_climate_inmet]{sus_climate_inmet()}} for multi-variable meteorological data (INMET)
\item \code{\link[=sus_climate_aggregate]{sus_climate_aggregate()}} for health-climate temporal integration
\item \code{\link[=sus_climate_fill_inmet]{sus_climate_fill_inmet()}} for ML-based gap filling
}
}
