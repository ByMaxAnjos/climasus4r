% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_grid_pdsi.R
\name{sus_grid_pdsi}
\alias{sus_grid_pdsi}
\title{Import Palmer Drought Severity Index (PDSI) for Brazilian Municipalities}
\usage{
sus_grid_pdsi(
  years = NULL,
  months = 1:12,
  source = c("terraclimate", "noaa_psl"),
  municipalities = NULL,
  agg_fun = "mean",
  crop_brazil = TRUE,
  use_cache = TRUE,
  cache_dir = "~/.climasus4r_cache/pdsi",
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{years}{Integer vector. Years to download.
TerraClimate: 1950–2025. NOAA PSL: 1850–2018.
\code{NULL} (default) = last two complete years.}

\item{months}{Integer vector (1–12). Months to include. Default \code{1:12}.}

\item{source}{Character. Data source: \code{"terraclimate"} (default) or
\code{"noaa_psl"}.}

\item{municipalities}{An \code{sf} POLYGON object (e.g., from
\code{\link[geobr:read_municipality]{geobr::read_municipality()}}). When provided, rasters are aggregated and a
\code{climasus_df} is returned. If \code{NULL}, returns cached NetCDF file paths.}

\item{agg_fun}{Character. Spatial aggregation for
\code{exactextractr::exact_extract()}. Default \code{"mean"} (area-weighted).}

\item{crop_brazil}{Logical. Crop global rasters to Brazil's bounding box.
Default \code{TRUE}.}

\item{use_cache}{Logical. Reuse cached NetCDF files and Parquet results.
Default \code{TRUE}.}

\item{cache_dir}{Character. Root cache directory.
Default \code{"~/.climasus4r_cache/pdsi"}.}

\item{lang}{Character. Message language: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
\itemize{
\item If \code{municipalities} is provided: a \code{climasus_df} with columns
\code{code_muni}, \code{date} (Date, first day of month), and \code{pdsi} (numeric,
unitless). Metadata: \code{stage = "climate"}, \code{type = "pdsi"}.
\item If \code{municipalities = NULL}: a named character vector of cached
NetCDF file paths.
}
}
\description{
\code{sus_grid_pdsi()} downloads Palmer Drought Severity Index (PDSI) data,
crops it to Brazil, spatially aggregates to municipalities, and returns a
\code{climasus_df} compatible with \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}} and \code{\link[=sus_climate_anomaly]{sus_climate_anomaly()}}.

PDSI (Palmer, 1965) quantifies cumulative moisture departures relative to
local climate normals using a two-layer soil water balance model. Values
range from approximately -10 (extreme drought) to +10 (extreme wet), with
operational categories at -2 (moderate drought) to -4 (extreme drought).
Health applications in Brazil:
\itemize{
\item Drought (PDSI < -2): malnutrition, diarrheal disease, mental health
stress, vector-borne disease in semi-arid Northeast and Amazônia
\item Wet periods (PDSI > 2): leptospirosis, hepatitis A, flooding
}

\strong{Available sources:}
\itemize{
\item \code{"terraclimate"} (default): TerraClimate monthly PDSI (Abatzoglou
et al., 2018; University of Idaho). Resolution 1/24° (~4 km), 1950–2025,
WGS84, no authentication. One NetCDF file (~165 MB) per year.
\item \code{"noaa_psl"}: Dai (2011) self-calibrated PDSI from NOAA Physical
Sciences Laboratory. Resolution 2.5°, 1850–2018, single global file
(~40 MB). Coarser but longer record.
}
}
\section{PDSI classification}{

\preformatted{
 PDSI >= +4.0 : Extremely wet
 PDSI  +3.0 to +3.99 : Very wet
 PDSI  +2.0 to +2.99 : Moderately wet
 PDSI  -1.99 to +1.99 : Near normal
 PDSI  -2.0 to -2.99 : Moderate drought  (D1)
 PDSI  -3.0 to -3.99 : Severe drought    (D2)
 PDSI <= -4.0 : Extreme drought           (D3-D4)
}
}

\section{Data sources}{

\itemize{
\item TerraClimate: Abatzoglou, J.T. et al. (2018). TerraClimate, a high-
resolution global dataset of monthly climate and climatic water balance
from 1958–2015. \emph{Scientific Data}, 5, 170191.
\doi{10.1038/sdata.2017.191}.
URL: \url{https://climate.northwestknowledge.net/TERRACLIMATE-DATA/}
\item NOAA PSL: Dai, A. (2011). Characteristics and trends in various
forms of the PDSI during 1900–2008. \emph{J. Geophys. Res.}, 116, D12115.
\doi{10.1029/2010JD015541}.
URL: \url{https://downloads.psl.noaa.gov/Datasets/dai_pdsi/}
}
}

\section{Packages required}{

\code{terra} and \code{exactextractr} (both in \code{Suggests}) are required when
\code{municipalities} is provided.
}

\examples{
\dontrun{
library(geobr)
mt_mun <- read_municipality(code_muni = "MT", year = 2020)

# TerraClimate PDSI for Mato Grosso, 2015-2022
pdsi_mt <- sus_grid_pdsi(
  years          = 2015:2022,
  months         = 1:12,
  municipalities = mt_mun,
  lang           = "pt"
)
sus_meta(pdsi_mt, "stage")  # "climate"
sus_meta(pdsi_mt, "type")   # "pdsi"

# Longer history with NOAA PSL (2.5 deg)
pdsi_hist <- sus_grid_pdsi(
  years          = 1960:2018,
  source         = "noaa_psl",
  municipalities = mt_mun,
  lang           = "en"
)

# Join to health data for DLNM
combined <- sus_grid_join(sih_mt, pdsi_mt)
}

}
\seealso{
\code{\link[=sus_climate_compute_spi]{sus_climate_compute_spi()}}, \code{\link[=sus_climate_compute_spei]{sus_climate_compute_spei()}},
\code{\link[=sus_grid_chirps]{sus_grid_chirps()}}, \code{\link[=sus_grid_join]{sus_grid_join()}}, \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}
}
