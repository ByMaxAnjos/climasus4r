% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_burden.R
\name{sus_mod_burden}
\alias{sus_mod_burden}
\title{Ranked Disease Burden Table Across Cities or Strata}
\usage{
sus_mod_burden(
  fits,
  component = "total",
  rank_by = NULL,
  top_n = NULL,
  nsim = 0L,
  alpha = 0.05,
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{fits}{Named list of \code{climasus_af}, \code{climasus_excess}, or
\code{climasus_dlnm} objects. Names become city/stratum labels in the output.
All elements must be the same class; mixing types is not allowed.}

\item{component}{Character. For \code{climasus_af} inputs: which heat/cold
component to display and rank. One of \code{"total"} (default), \code{"heat"},
\code{"cold"}, or \code{"all"} (all three components; ranking is always derived
from the \code{"total"} component). Ignored for \code{climasus_excess} inputs.}

\item{rank_by}{Character or \code{NULL}. Metric for ranking cities.
For AF inputs: \code{"an"} (attributable number, default) or \code{"af_pct"}
(attributable fraction percent). For excess inputs: \code{"excess"} (default)
or \code{"excess_pct"}. \code{NULL} selects the default for the detected input type.}

\item{top_n}{Integer or \code{NULL}. Keep only the top-N cities after ranking.
\code{NULL} (default) retains all cities.}

\item{nsim}{Integer. Monte Carlo simulations used when auto-computing AF
from \code{climasus_dlnm} inputs. \code{0L} (default) uses the faster delta-method CI.}

\item{alpha}{Numeric. Significance level for confidence intervals.
Default \code{0.05}.}

\item{lang}{Character. Language for messages: \code{"pt"} (default), \code{"en"},
\code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{climasus_burden} object (named list) with:
\describe{
\item{\verb{$burden_table}}{Tibble with one row per city (three rows per city
when \code{component = "all"}). For AF inputs: \code{city}, \code{component},
\code{n_cases}, \code{an}, \code{an_lo}, \code{an_hi}, \code{af_pct}, \code{af_pct_lo},
\code{af_pct_hi}, \code{rank}, \code{pct_of_total}. For excess inputs: \code{city},
\code{n_days}, \code{observed}, \code{expected}, \code{excess}, \code{excess_lo},
\code{excess_hi}, \code{excess_pct}, \code{rank}, \code{pct_of_total}.}
\item{\verb{$concentration}}{City-level tibble with \code{city}, \code{rank},
\code{pct_of_total}, and \code{cumulative_pct} for Lorenz-style concentration
analysis. Always one row per city, based on the total component.}
\item{\verb{$total_burden}}{Named list with aggregate statistics: \code{an_total}
and \code{af_pct_avg} for AF inputs; \code{excess_total} and \code{excess_pct_avg}
for excess inputs. Also contains \code{top_city} and the top city's metric.}
\item{\verb{$meta}}{Named list of all parameters used in this call.}
}
}
\description{
Aggregates city-level attributable fraction or excess mortality/morbidity
results into a ranked burden table showing each city's contribution to the
total population burden. Accepts \code{climasus_af}, \code{climasus_excess}, or
\code{climasus_dlnm} objects (DLNM fits are automatically converted to AF via
\code{\link[=sus_mod_af]{sus_mod_af()}}). Produces a concentration curve suitable for Lorenz-style
inequality analysis: how unevenly burden is distributed across cities.
}
\examples{
\dontrun{
# Rank cities by attributable number (AN)
af_list <- list(
  fortaleza = sus_mod_af(fit_fortaleza, verbose = FALSE),
  recife    = sus_mod_af(fit_recife,    verbose = FALSE),
  salvador  = sus_mod_af(fit_salvador,  verbose = FALSE)
)
burden <- sus_mod_burden(af_list, lang = "pt")
print(burden)
tidy(burden)

# Show heat/cold split, keep top 5 cities
sus_mod_burden(af_list, component = "all", top_n = 5L)

# From DLNM fits directly (AF auto-computed via delta method)
burden2 <- sus_mod_burden(dlnm_list, nsim = 0L, lang = "en")

# Excess-based ranking
exc_list <- list(
  sp  = sus_mod_excess(fit_sp,  method = "from_dlnm", verbose = FALSE),
  rj  = sus_mod_excess(fit_rj,  method = "from_dlnm", verbose = FALSE)
)
sus_mod_burden(exc_list, rank_by = "excess_pct")
}

}
\seealso{
\code{\link[=sus_mod_af]{sus_mod_af()}}, \code{\link[=sus_mod_excess]{sus_mod_excess()}}, \code{\link[=sus_mod_pool]{sus_mod_pool()}},
\code{\link[=sus_mod_metaregression]{sus_mod_metaregression()}}, \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}
}
