% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_grid_koppen.R
\name{sus_grid_koppen}
\alias{sus_grid_koppen}
\title{Assign Kopppen-Geiger Climate Zones to Brazilian Municipalities}
\usage{
sus_grid_koppen(
  data,
  mode = "approx",
  koppen_sf = NULL,
  as_factor = TRUE,
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{climasus_df} or \code{data.frame} containing a \code{code_muni} column
(6- or 7-digit IBGE municipality code), \strong{or} an \code{sf} object of Brazilian
municipalities.}

\item{mode}{Character. Assignment mode: \code{"approx"} (default, no extra
dependencies) or \code{"exact"} (requires \code{sf} and \code{koppen_sf}).}

\item{koppen_sf}{An \code{sf} polygon object with a column named \code{koppen}
containing the zone codes (\code{"Af"}, \code{"Am"}, etc.). Required when
\code{mode = "exact"}. The Alvares et al. (2013) shapefile is available at
\url{https://doi.org/10.1127/0941-2948/2013/0507}. Ignored for
\code{mode = "approx"}.}

\item{as_factor}{Logical. Return \code{zona_koppen} as an ordered factor with
canonical level order (\verb{Af < Am < As < Aw < BSh < Cf < Cw})?
Default \code{TRUE}. Set to \code{FALSE} to get a plain character column.}

\item{lang}{Character. Language for messages: \code{"pt"} (default), \code{"en"},
\code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
The input object with an additional column \code{zona_koppen}. If the
input was a \code{climasus_df}, the returned object preserves its class and
\code{sus_meta} attribute with the history updated.
}
\description{
Adds a \code{zona_koppen} column to a \code{climasus_df} (or plain \code{data.frame}) with
municipality-level Koppen-Geiger climate classification following
Alvares et al. (2013), who produced the first high-resolution (1 km) Koppen
map for Brazil.
}
\details{
Two assignment modes are available:
\itemize{
\item \strong{\code{"approx"}} (default): rule-based assignment from municipality centroid
coordinates stored in the built-in \code{municipio_meta} table. Fast and
dependency-free, with ~85\% accuracy at the municipal level.
\item \strong{\code{"exact"}}: spatial join between municipality centroids and a user-
supplied \code{sf} polygon layer (\code{koppen_sf}). Produces exact results when the
original Alvares et al. (2013) shapefile is provided.
}
}
\section{Koppen zones for Brazil (Alvares et al. 2013)}{

\describe{
\item{\strong{Af}}{Tropical humid (equatorial) -- Amazon / NE coast.}
\item{\strong{Am}}{Tropical monsoon -- North / Center-West.}
\item{\strong{As}}{Tropical summer dry -- NE coast.}
\item{\strong{Aw}}{Tropical winter dry (savanna) -- Central Brazil.}
\item{\strong{BSh}}{Hot semi-arid -- NE \emph{sertao}.}
\item{\strong{Cf}}{Subtropical humid -- South / coastal SE.}
\item{\strong{Cw}}{Subtropical winter dry -- SE plateau / Center-West uplands.}
}
}

\examples{
\dontrun{
library(climasus4r)

# With a climasus_df that has code_muni
df_with_koppen <- sus_grid_koppen(df_aggregated, mode = "approx")
table(df_with_koppen$zona_koppen)

# Exact mode with Alvares et al. 2013 shapefile
library(sf)
kop_sf <- sf::read_sf("koppen_brazil_alvares2013.shp")
names(kop_sf)[names(kop_sf) == "zone"] <- "koppen"  # rename if needed
df_exact <- sus_grid_koppen(df_aggregated, mode = "exact", koppen_sf = kop_sf)
}

}
\references{
Alvares, C.A., Stape, J.L., Sentelhas, P.C., Goncalves, J.L.M., &
Sparovek, G. (2013). Koppen's climate classification map for Brazil.
\emph{Meteorologische Zeitschrift}, 22(6), 711-728.
\doi{10.1127/0941-2948/2013/0507}
}
\seealso{
\code{\link[=sus_grid_era5]{sus_grid_era5()}}, \code{\link[=sus_mod_casecrossover]{sus_mod_casecrossover()}}, \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}
}
