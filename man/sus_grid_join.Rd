% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_grid_join.R
\name{sus_grid_join}
\alias{sus_grid_join}
\title{Join Gridded Environmental Data to Health Data}
\usage{
sus_grid_join(
  health_data,
  grid_data,
  by = c("code_muni", "date"),
  type_out = NULL,
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{health_data}{A \code{climasus_df} at stage \code{"spatial"}, \code{"aggregate"}, or
\code{"census"}. Must contain \code{code_muni} (character) and \code{date} (Date) columns.}

\item{grid_data}{A \code{climasus_df} at stage \code{"climate"} produced by any
\verb{sus_grid_*()} function. Must contain \code{code_muni} and \code{date} columns and
at least one environmental variable column.}

\item{by}{Character vector. Join key columns. Default \code{c("code_muni", "date")}.
Change to \code{c("code_muni")} if joining annual grid data (e.g., PRODES) to
monthly health data — the join will broadcast the annual value to all months.}

\item{type_out}{Character. Overrides \code{sus_meta$type} in the result.
\code{NULL} (default) inherits the type from \code{grid_data}.}

\item{lang}{Character. Message language: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{climasus_df} with all columns from \code{health_data} plus the
environmental variable columns from \code{grid_data}. Metadata:
\code{stage = "climate"}, \code{type} inherited from \code{grid_data} (or \code{type_out}).
Rows from \code{health_data} with no matching grid row receive \code{NA} for grid
columns.
}
\description{
\code{sus_grid_join()} merges the municipality × date output of any
\verb{sus_grid_*()} function (ERA5, CHIRPS, GHAP, CAMS, fires, PRODES, …) with
a health \code{climasus_df} produced by \code{\link[=sus_spatial_join]{sus_spatial_join()}} or
\code{\link[=sus_data_aggregate]{sus_data_aggregate()}}. The result is a single \code{climasus_df} at
\code{stage = "climate"} ready for \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}, \code{\link[=sus_climate_anomaly]{sus_climate_anomaly()}},
and \code{\link[=sus_mod_vulnerability_index]{sus_mod_vulnerability_index()}}.

This function is the bridge between the gridded environmental pipeline
(\verb{sus_grid_*}) and the health pipeline (\code{sus_data_import} → … →
\code{sus_spatial_join}), equivalent to the role \code{\link[=sus_climate_aggregate]{sus_climate_aggregate()}} plays
for station-based INMET data.
}
\section{Temporal granularity mismatch}{

If \code{health_data} has monthly dates (\code{YYYY-MM-01}) and \code{grid_data} has daily
dates, the join will produce \code{NA}s for most rows. In that case, first
aggregate \code{grid_data} to monthly resolution with
\code{\link[=sus_climate_aggregate]{sus_climate_aggregate()}} or by passing \code{by = c("code_muni")} (annual
grid data only) or summarising manually before calling \code{sus_grid_join()}.
}

\examples{
\dontrun{
library(geobr)
mt_mun <- read_municipality(code_muni = "MT", year = 2020)

# Health data (SIH respiratory hospitalisations)
sih_agg <- sus_data_import(system = "SIH", years = 2020, uf = "MT") |>
  sus_data_clean_encoding() |>
  sus_data_standardize() |>
  sus_data_filter_cid(cid_group = "respiratorio") |>
  sus_data_aggregate() |>
  sus_spatial_join(municipalities = mt_mun)

# Gridded climate data
era5 <- sus_grid_era5(
  years          = 2020,
  municipalities = mt_mun,
  vars           = c("t2m", "tp")
)

# Join: one step to connect health + climate
combined <- sus_grid_join(sih_agg, era5)
sus_meta(combined, "stage")  # "climate"
sus_meta(combined, "type")   # "era5_land"

# Annual deforestation joined to monthly health data (no date in by)
prodes <- sus_grid_prodes(years = 2020, biomes = "Amazon",
                           uf = "MT", municipalities = mt_mun)
combined2 <- sus_grid_join(sih_agg, prodes, by = "code_muni")
}

}
\seealso{
\code{\link[=sus_grid_era5]{sus_grid_era5()}}, \code{\link[=sus_grid_chirps]{sus_grid_chirps()}}, \code{\link[=sus_grid_fires]{sus_grid_fires()}},
\code{\link[=sus_grid_prodes]{sus_grid_prodes()}}, \code{\link[=sus_climate_aggregate]{sus_climate_aggregate()}}, \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}
}
