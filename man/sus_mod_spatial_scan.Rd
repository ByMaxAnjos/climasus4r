% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_spatial_scan.R
\name{sus_mod_spatial_scan}
\alias{sus_mod_spatial_scan}
\title{Kulldorff Circular Scan Statistic for Spatial Cluster Detection}
\usage{
sus_mod_spatial_scan(
  df,
  cases,
  population,
  municipalities,
  expected = NULL,
  max_pop_frac = 0.5,
  n_simulations = 999L,
  alpha = 0.05,
  lang = c("pt", "en", "es"),
  verbose = TRUE
)
}
\arguments{
\item{df}{A \code{data.frame} (or \code{climasus_df}) containing at minimum:
a \code{code_muni} column (character or integer) and the columns named by
\code{cases}, \code{population}, and optionally \code{expected}.}

\item{cases}{Character. Name of the column in \code{df} with case counts
(non-negative integer or numeric).}

\item{population}{Character. Name of the column in \code{df} with the
at-risk population denominator.}

\item{municipalities}{An \code{sf} object with POLYGON or MULTIPOLYGON
geometry and a \code{code_muni} column used to join to \code{df}.}

\item{expected}{Character or \code{NULL}. Name of the column in \code{df} with
the expected number of cases under the null model. When \code{NULL}
(default), \code{\link[SpatialEpi:kulldorff]{SpatialEpi::kulldorff()}} computes expected counts
internally from the overall rate.}

\item{max_pop_frac}{Numeric in (0, 1]. Maximum fraction of the total
population that a single cluster window may contain. Default \code{0.5}.}

\item{n_simulations}{Positive integer. Number of Monte Carlo simulations
for hypothesis testing. Default \code{999}. Increase to \code{9999} for
publication-quality p-values.}

\item{alpha}{Numeric in (0, 1). Significance level for secondary cluster
filtering. Default \code{0.05}.}

\item{lang}{Character. Output language: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{climasus_spatial_scan} object (named list) with:
\describe{
\item{\verb{$most_likely_cluster}}{Named list:
\code{location_ids} (character vector of \code{code_muni} in the cluster),
\code{observed} (numeric), \code{expected} (numeric), \code{RR} (relative risk),
\code{log_lik} (log-likelihood ratio), \code{p_value} (Monte Carlo p-value).}
\item{\verb{$secondary_clusters}}{List of lists with the same structure as
\code{most_likely_cluster}, one per significant secondary cluster. Empty
list when no secondary cluster is significant.}
\item{\verb{$n_clusters}}{Integer. Total number of significant clusters
(1 + length of \code{secondary_clusters}), or 0 if the most-likely cluster
itself is not significant.}
\item{\verb{$data}}{Tibble with one row per municipality: \code{code_muni},
\code{cases}, \code{population}, \code{expected}, and a logical column \code{in_mlc}
indicating membership in the most-likely cluster.}
\item{\verb{$meta}}{Named list of analysis parameters.}
\item{\verb{$call}}{The matched call.}
}
}
\description{
Applies the Kulldorff circular spatial scan statistic to detect geographic
clusters of disease excess. The most-likely cluster and any significant
secondary clusters are identified by Monte Carlo hypothesis testing. The
analysis requires a tabular dataset with case counts and population
denominators at the municipality level, plus an \code{sf} polygon layer
providing municipality boundaries.
}
\section{Methodology}{

The scan statistic imposes circular windows of variable radius over the
map. For each window the likelihood ratio (LR) under a Poisson model
(observed vs. expected cases inside vs. outside the window) is computed.
The window with the maximum LR is the most-likely cluster. Its p-value is
obtained by comparing to the empirical distribution of max-LR under
\code{n_simulations} Monte Carlo permutations. Secondary clusters are non-
overlapping windows with LR > the \code{alpha}-level critical value from the
same Monte Carlo distribution.

Computations are delegated to \code{\link[SpatialEpi:kulldorff]{SpatialEpi::kulldorff()}}. Municipality
centroids are derived from the polygon geometry using
\code{\link[sf:geos_unary]{sf::st_centroid()}} after reprojecting to WGS 84 (EPSG 4326) so that
longitude/latitude coordinates are passed to \code{kulldorff()}.
}

\examples{
\dontrun{
library(geobr)
library(dplyr)

# Download municipality polygons for Minas Gerais (state code 31)
muni_sf <- geobr::read_municipality(code_muni = 31, year = 2020)
muni_sf <- dplyr::rename(muni_sf, code_muni = code_muni)

# Build a synthetic data.frame
set.seed(42)
df_cases <- tibble::tibble(
  code_muni  = as.character(muni_sf$code_muni),
  cases      = rpois(nrow(muni_sf), lambda = 5),
  population = sample(5000:200000, nrow(muni_sf), replace = TRUE)
)

result <- sus_mod_spatial_scan(
  df            = df_cases,
  cases         = "cases",
  population    = "population",
  municipalities = muni_sf,
  n_simulations = 199,
  lang          = "pt"
)

print(result)
result$most_likely_cluster
result$secondary_clusters
}

}
\references{
Kulldorff, M. & Nagarwalla, N. (1995). Spatial disease clusters:
detection and inference. \emph{Statistics in Medicine}, 14(8), 799--810.
\doi{10.1002/sim.4780140809}

Kulldorff, M. (1997). A spatial scan statistic. \emph{Communications in
Statistics - Theory and Methods}, 26(6), 1481--1496.
\doi{10.1080/03610929708831995}

Kim, A. Y. & Wakefield, J. (2010). R data and methods for spatial
epidemiology: the SpatialEpi package. University of Washington.
}
\seealso{
\code{\link[=sus_spatial_join]{sus_spatial_join()}}, \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}, \code{\link[=sus_mod_af]{sus_mod_af()}}
}
