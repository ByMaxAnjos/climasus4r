% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_climate_compute_indicators.R
\name{sus_climate_compute_indicators}
\alias{sus_climate_compute_indicators}
\title{Compute Bioclimatic and Thermal Stress Indicators}
\usage{
sus_climate_compute_indicators(
  df,
  indicators = "all",
  region = "auto",
  datetime_col = NULL,
  station_col = NULL,
  apply_validity_mask = TRUE,
  confidence_flags = NULL,
  custom_thresholds = NULL,
  keep_source_vars = FALSE,
  verify_physics = TRUE,
  compute_uncertainty = FALSE,
  verbose = TRUE,
  lang = "pt",
  use_cache = TRUE,
  cache_dir = "~/.climasus4r_cache/spatial"
)
}
\arguments{
\item{df}{Data frame or tibble from \code{sus_climate_inmet()} or \code{sus_climate_fill()}.}

\item{indicators}{Character vector of indicator codes or \code{"all"} (default).}

\item{region}{Character. \code{"auto"} (default, standard thresholds) or a
Brazilian biome: \code{"amazon"}, \code{"cerrado"}, \code{"caatinga"},
\code{"atlantic_forest"}, \code{"pampa"}, \code{"pantanal"}, \code{"southeast"}, \code{"south"}.}

\item{datetime_col}{Character or \code{NULL} (auto-detected).}

\item{station_col}{Character or \code{NULL} (auto-detected).}

\item{apply_validity_mask}{Logical (default \code{TRUE}). When \code{region=="none"},
masks HI, WCET, WCT outside their valid meteorological domains with \code{NA}.}

\item{confidence_flags}{Logical. Add \verb{_flag_extreme}, \verb{_flag_high},
\verb{_flag_low} columns. Default \code{TRUE} when region is set, \code{FALSE} otherwise.}

\item{custom_thresholds}{Named list to override regional defaults.}

\item{keep_source_vars}{Logical (default \code{FALSE}).}

\item{verify_physics}{Logical (default \code{TRUE}). Check solar radiation
against extraterrestrial limits; requires \code{latitude} column.}

\item{compute_uncertainty}{Logical (default \code{FALSE}). Monte Carlo 95\% CI.}

\item{verbose}{Logical (default \code{TRUE}).}

\item{lang}{Character: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses cached spatial data to avoid
re-downloads and improve performance.}

\item{cache_dir}{Character string specifying the directory to store cached files.
Default is \code{"~/.climasus4r_cache/spatial"}.}
}
\value{
A \code{tibble} of class \code{climasus_df} containing indicator columns,
optional flag/CI columns, and a \code{sus_meta} attribute.
}
\description{
\code{sus_climate_compute_indicators()} calculates a comprehensive set of
bioclimatic and thermal-stress indices from INMET station data (output of
\code{sus_climate_inmet()} or \code{sus_climate_fill()}).

\strong{Scientific robustness highlights:}
\itemize{
\item WBGT uses a dual wet-bulb estimate (Liljegren + Bernard/Pourmoghani)
averaged for numerical robustness; solar radiation \code{NA} treated as 0
(no solar load at night) so all hours yield a valid value.
\item HI implements full Rothfusz (1990) regression with both NWS humidity
adjustments and a regionally adapted validity threshold.
\item UTCI uses a Fiala-polynomial-inspired multi-term regression (vapour
pressure, logarithmic wind, radiation linear + non-linear,
wind vs radiation interaction).
\item PET is based on a MEMI-style energy-balance approximation with
seasonal clothing adjustment and regional correction.
\item Solar radiation undergoes optional physical consistency verification
against extraterrestrial radiation limits.
\item Diurnal range is a true daily aggregate (Tmax - Tmin per calendar day)
broadcast to all hourly rows of that day.
\item Multi-level confidence flags (\verb{_flag_extreme}, \verb{_flag_high},
\verb{_flag_low}) replace a single \verb{_out_of_range} flag.
\item Optional 200-simulation Monte Carlo uncertainty estimation.
}

\strong{Available indicators:}\tabular{lll}{
   Code \tab Output column \tab Requires \cr
   \code{wbgt} \tab \code{wbgt_c} \tab T, RH, SR, WS \cr
   \code{hi} \tab \code{hi_c} \tab T, RH \cr
   \code{thi} \tab \code{thi_c} \tab T, RH \cr
   \code{wcet} \tab \code{wcet_c} \tab T, WS \cr
   \code{wct} \tab \code{wct_c} \tab T, WS \cr
   \code{et} \tab \code{et_c} \tab T, RH, WS \cr
   \code{utci} \tab \code{utci_c} \tab T, RH, WS, SR \cr
   \code{pet} \tab \code{pet_c} \tab T, RH, WS, SR \cr
   \code{cdd} \tab \code{cdd_c} \tab T \cr
   \code{hdd} \tab \code{hdd_c} \tab T \cr
   \code{gdd} \tab \code{gdd_c} \tab T \cr
   \code{diurnal_range} \tab \code{diurnal_range_c} \tab T (daily agg.) \cr
   \code{vapor_pressure} \tab \code{vapor_pressure_kpa} \tab T, RH \cr
   \code{heat_stress_risk} \tab \code{heat_stress_risk} \tab T, RH, WS, SR \cr
   \code{koppen_humidity} \tab \code{koppen_humidity} \tab RH \cr
}


\emph{(Where: T=\code{tair_dry_bulb_c}, RH=\code{rh_mean_porc}, SR=\code{sr_kj_m2}, WS=\code{ws_2_m_s})}
}
\section{Scientific References}{

\itemize{
\item Liljegren et al. (2008) \emph{J. Occup. Environ. Hyg.} 5(10):645-655.
\item Bernard & Pourmoghani (1999) \emph{AIHAJ} 60(1):32-37.
\item Rothfusz (1990) NWS Tech. Attachment SR/SSD 90-23.
\item Thom (1959) \emph{Weatherwise} 12(2):57-59.
\item Environment Canada (2001) Wind Chill Index.
\item Brode et al. (2012) \emph{Int. J. Biometeorol.} 56(4):481-494.
\item Matzarakis et al. (1999) \emph{Int. J. Biometeorol.} 43:76-84.
\item Magnus (1844) / Tetens (1930) saturation vapour pressure.
}
}

\examples{
\dontrun{
result <- sus_climate_compute_indicators(df_clima)

result <- sus_climate_compute_indicators(
  df_clima, region = "amazon", confidence_flags = TRUE
)

result <- sus_climate_compute_indicators(
  df_clima,
  region              = "auto",
  verify_physics      = TRUE,
  compute_uncertainty = TRUE,
  keep_source_vars    = TRUE
)

df_clima |>
  sus_climate_fill(target_var = "all") |>
  sus_climate_compute_indicators(indicators = c("wbgt", "hi", "utci"))
}
}
