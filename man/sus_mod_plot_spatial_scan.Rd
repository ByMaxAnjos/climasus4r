% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_plot_spatial_scan.R
\name{sus_mod_plot_spatial_scan}
\alias{sus_mod_plot_spatial_scan}
\title{Choropleth Map of Kulldorff Spatial Scan Cluster Results}
\usage{
sus_mod_plot_spatial_scan(
  x,
  municipalities,
  show_rr = TRUE,
  alpha = 0.05,
  title = NULL,
  lang = "pt",
  ...
)
}
\arguments{
\item{x}{A \code{climasus_spatial_scan} object returned by
\code{\link[=sus_mod_spatial_scan]{sus_mod_spatial_scan()}}.}

\item{municipalities}{An \code{sf} object with POLYGON or MULTIPOLYGON geometry
containing a \code{code_muni} column that matches the identifiers stored in
\code{x$most_likely_cluster$location_ids} and \code{x$secondary_clusters}.}

\item{show_rr}{Logical.  \code{TRUE} (default) colours municipalities by
Relative Risk using a continuous diverging scale.  \code{FALSE} uses
categorical cluster-membership colours.}

\item{alpha}{Numeric in (0, 1).  Significance threshold used to decide
which cluster borders to emphasise.  Must match the value used when
running \code{\link[=sus_mod_spatial_scan]{sus_mod_spatial_scan()}}.  Default \code{0.05}.}

\item{title}{Character or \code{NULL}.  Plot title.  If \code{NULL} (default) a
localised default title is used.}

\item{lang}{Character.  Output language: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{...}{Currently unused; reserved for future extensions.}
}
\value{
A \code{ggplot} object.  Assign to a variable to modify further or pass
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
}
\description{
Renders a choropleth map from a \code{climasus_spatial_scan} object produced by
\code{\link[=sus_mod_spatial_scan]{sus_mod_spatial_scan()}}.  Each municipality polygon is coloured either by
its cluster's Relative Risk (RR, continuous diverging scale) or by a
categorical cluster label, making it straightforward to communicate the
location, extent, and intensity of spatial health clusters.
}
\section{Colour encoding}{

When \code{show_rr = TRUE} (default), the fill variable is the numeric RR of the
cluster that each municipality belongs to.  Background municipalities
(\code{cluster_id == 0}) receive \code{NA} and are rendered in \code{"grey80"}.  A
diverging gradient (navy to white to red) centred on RR = 1 is used.
Municipalities belonging to a significant cluster (p-value < \code{alpha})
additionally receive a thicker border (\code{linewidth = 1.5}) to distinguish
them from the most-likely cluster when it is not itself significant.

When \code{show_rr = FALSE}, municipalities are filled with categorical colours:
the most-likely cluster, up to nine secondary clusters, and the background
each receive a distinct colour via \code{\link[ggplot2:scale_manual]{ggplot2::scale_fill_manual()}}.
}

\examples{
\dontrun{
library(geobr)
library(dplyr)

muni_sf <- geobr::read_municipality(code_muni = 31, year = 2020)
muni_sf <- dplyr::rename(muni_sf, code_muni = code_muni)

set.seed(1)
df_cases <- tibble::tibble(
  code_muni  = as.character(muni_sf$code_muni),
  cases      = rpois(nrow(muni_sf), 5),
  population = sample(5000:200000, nrow(muni_sf), replace = TRUE)
)

result <- sus_mod_spatial_scan(
  df             = df_cases,
  cases          = "cases",
  population     = "population",
  municipalities = muni_sf,
  n_simulations  = 199L,
  lang           = "pt"
)

# Continuous RR map (default)
sus_mod_plot_spatial_scan(result, municipalities = muni_sf, lang = "pt")

# Categorical cluster-membership map
sus_mod_plot_spatial_scan(result, municipalities = muni_sf,
                          show_rr = FALSE, lang = "en")
}

}
\seealso{
\code{\link[=sus_mod_spatial_scan]{sus_mod_spatial_scan()}}, \code{\link[=sus_spatial_join]{sus_spatial_join()}},
\code{\link[=sus_mod_plot_vulnerability]{sus_mod_plot_vulnerability()}}
}
