% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_spatial_moran.R
\name{sus_mod_spatial_moran}
\alias{sus_mod_spatial_moran}
\title{Global Moran's I and LISA Local Autocorrelation for Health Outcomes}
\usage{
sus_mod_spatial_moran(
  df,
  outcome,
  W,
  municipalities = NULL,
  permutations = 999L,
  alpha = 0.05,
  adjust_p = c("fdr", "bonferroni", "none"),
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{df}{A \code{data.frame} or \code{climasus_df} containing at least the columns
\code{code_muni} (7-digit IBGE municipality code) and the outcome column
named by \code{outcome}. The row order must correspond to the spatial units
encoded in \code{W} unless \code{municipalities} is supplied to subset and reorder.}

\item{outcome}{Character. Name of the numeric outcome column in \code{df}
(e.g. \code{"deaths"}, \code{"rate_100k"}, \code{"hospitalization_rate"}).}

\item{W}{A \code{climasus_weights} object produced by
\code{sus_mod_spatial_weights()}. Must contain a \code{listw} element (a
\code{listw}-class object from \strong{spdep}) whose length matches \code{nrow(df)} after
any filtering by \code{municipalities}.}

\item{municipalities}{Optional character or integer vector of \code{code_muni}
values to include. When supplied the function subsets and reorders \code{df}
to match this vector before computation. Default \code{NULL} (use all rows).}

\item{permutations}{Positive integer. Number of Monte Carlo permutations
for both global and local inference. Default \code{999}.}

\item{alpha}{Numeric in (0, 1). Significance threshold applied to
p-adjusted local Moran p-values when assigning quadrant labels.
Default \code{0.05}.}

\item{adjust_p}{Character. Method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}} for
multiple-testing correction of local Moran p-values. One of \code{"fdr"}
(default), \code{"bonferroni"}, or \code{"none"}.}

\item{lang}{Character. Output language for messages: \code{"pt"} (default),
\code{"en"}, or \code{"es"}.}

\item{verbose}{Logical. If \code{TRUE} (default), print progress messages via
\strong{cli}.}
}
\value{
An object of class \code{c("climasus_spatial_moran", "list")} with the
following named elements:
\describe{
\item{\verb{$global}}{One-row \code{data.frame} with columns \code{I}, \code{E.I},
\code{Var.I}, \code{Z.I}, \code{p_value} (analytical), and \code{p_simulated}
(permutation-based).}
\item{\verb{$local}}{\code{data.frame} with one row per spatial unit and columns
\code{code_muni}, \code{Ii} (local Moran statistic), \code{Z.Ii} (z-score),
\code{p_raw} (unit-level permutation p), \code{p_adj} (adjusted p), and
\code{quadrant} (factor: \code{"HH"}, \code{"LL"}, \code{"HL"}, \code{"LH"}, \code{"NS"}).}
\item{\verb{$n_HH}}{Integer. Number of significant HH (hotspot) units.}
\item{\verb{$n_LL}}{Integer. Number of significant LL (coldspot) units.}
\item{\verb{$n_HL}}{Integer. Number of significant HL spatial outlier units.}
\item{\verb{$n_LH}}{Integer. Number of significant LH spatial outlier units.}
\item{\verb{$outcome_name}}{Character. The \code{outcome} argument value.}
\item{\verb{$call}}{The matched function call.}
}
}
\description{
Computes Moran's I global spatial autocorrelation statistic and Local
Indicators of Spatial Association (LISA) for a numeric health outcome
measured at municipality level. The global statistic tests whether the
outcome is spatially clustered across the study region; the local statistics
identify significant \strong{High-High} (hotspots), \strong{Low-Low} (coldspots),
\strong{High-Low}, and \strong{Low-High} spatial outlier clusters.
}
\section{Global statistic}{


Moran's I is computed via permutation inference using
\code{\link[spdep:moran.mc]{spdep::moran.mc()}}. The observed statistic is compared against the
empirical null distribution obtained by randomly permuting the outcome
values across spatial units. The reported \code{p_simulated} is the proportion
of permuted values as extreme as or more extreme than the observed I.
}

\section{Local statistic (LISA)}{


Local Moran statistics are computed via \code{\link[spdep:localmoran]{spdep::localmoran_perm()}}, which
uses conditional permutation to generate unit-specific null distributions.
Each observation is assigned a LISA quadrant based on its standardised
value and the standardised spatial lag:

\describe{
\item{\code{HH}}{High value surrounded by high neighbours (hotspot).}
\item{\code{LL}}{Low value surrounded by low neighbours (coldspot).}
\item{\code{HL}}{High value surrounded by low neighbours (spatial outlier).}
\item{\code{LH}}{Low value surrounded by high neighbours (spatial outlier).}
\item{\code{NS}}{Not significant at the specified \code{alpha} level.}
}
}

\examples{
\dontrun{
# Requires spdep and a climasus_weights object W
library(climasus4r)

# W <- sus_mod_spatial_weights(shp, style = "W")
result <- sus_mod_spatial_moran(
  df           = my_df,
  outcome      = "deaths",
  W            = W,
  permutations = 999,
  alpha        = 0.05,
  adjust_p     = "fdr",
  lang         = "pt"
)
print(result)

# Access local clusters
result$local[result$local$quadrant == "HH", ]
}

}
\references{
Moran, P. A. P. (1950). Notes on continuous stochastic phenomena.
\emph{Biometrika}, 37(1-2), 17-23.

Anselin, L. (1995). Local indicators of spatial association---LISA.
\emph{Geographical Analysis}, 27(2), 93-115.

Bivand, R. S., Pebesma, E., & Gomez-Rubio, V. (2013).
\emph{Applied Spatial Data Analysis with R} (2nd ed.). Springer.
}
\seealso{
\code{\link[=sus_mod_spatial_weights]{sus_mod_spatial_weights()}}, \code{\link[=sus_spatial_join]{sus_spatial_join()}}
}
