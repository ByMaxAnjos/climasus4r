% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_grid_chirps.R
\name{sus_grid_chirps}
\alias{sus_grid_chirps}
\title{Import CHIRPS Rainfall Data for Brazilian Municipalities}
\usage{
sus_grid_chirps(
  resolution = "monthly",
  years = NULL,
  months = 1:12,
  municipalities = NULL,
  agg_fun = "mean",
  crop_brazil = TRUE,
  use_cache = TRUE,
  cache_dir = "~/.climasus4r_cache/chirps",
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{resolution}{Character. Temporal resolution of source files:
\itemize{
\item \code{"monthly"} (default) — one file per month (~5–20 MB each);
returns mm/month.
\item \code{"daily"} — one file per day (~3 MB each); returns mm/day.
Recommended only for short periods; a full year = 365 downloads.
\item \code{"annual"} — one file per year; returns mm/year.
}}

\item{years}{Integer vector. Years to download. Coverage: 1981 to present
for daily/monthly; 1981–2024 for annual. \code{NULL} defaults to the last
two complete years.}

\item{months}{Integer vector (1–12). Months to include for daily and monthly
resolutions. Ignored for annual. Default \code{1:12}.}

\item{municipalities}{An \code{sf} POLYGON object (e.g., from
\code{\link[geobr:read_municipality]{geobr::read_municipality()}}). When provided, rasters are aggregated and a
\code{climasus_df} is returned. If \code{NULL}, a named character vector of cached
file paths is returned instead.}

\item{agg_fun}{Character. Spatial aggregation function for
\code{exactextractr::exact_extract()}. Default \code{"mean"} (area-weighted mean).
For rainfall totals over a municipality use \code{"sum"}.}

\item{crop_brazil}{Logical. Crop global rasters to Brazil's bounding box
before aggregation. Reduces memory usage significantly. Default \code{TRUE}.}

\item{use_cache}{Logical. Reuse previously downloaded raster files and
aggregated Parquet caches. Default \code{TRUE}.}

\item{cache_dir}{Character. Root cache directory.
Default \code{"~/.climasus4r_cache/chirps"}.}

\item{lang}{Character. Message language: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
\itemize{
\item If \code{municipalities} is provided: a \code{climasus_df} with columns
\code{code_muni} (character), \code{date} (Date), and \code{rainfall_chirps_mm}
(numeric). Metadata: \code{stage = "climate"}, \code{type = "chirps"}.
\item If \code{municipalities = NULL}: a named character vector of paths to
the cached GeoTIFF/gz files.
}
}
\description{
\code{sus_grid_chirps()} downloads CHIRPS v2.0 precipitation data, crops it to
Brazil, spatially aggregates to municipalities, and returns a \code{climasus_df}
compatible with \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}} and \code{\link[=sus_climate_anomaly]{sus_climate_anomaly()}}.

CHIRPS (Climate Hazards Group InfraRed Precipitation with Station data) is a
quasi-global, 0.05\eqn{^\circ} (~5 km) daily rainfall dataset from UCSB CHC
covering 1981 to present. It combines satellite imagery with station data and
is considered the best freely available high-resolution rainfall product for
Brazil — particularly for leptospirosis, diarrheal disease, and dengue
analyses where precipitation is the key exposure.

No authentication is required.
}
\section{Units}{

\itemize{
\item Daily: mm/day
\item Monthly: mm/month (cumulative)
\item Annual: mm/year (cumulative)
}
CHIRPS missing/no-data value (-9999) is automatically converted to \code{NA}.
}

\section{Data source}{

Funk, C. et al. (2015). The climate hazards infrared precipitation with
stations — a new environmental record for monitoring extremes.
\emph{Scientific Data}, 2, 150066. \doi{10.1038/sdata.2015.66}\cr
Data: \url{https://data.chc.ucsb.edu/products/CHIRPS-2.0/}
}

\section{Packages required}{

Spatial aggregation requires \code{terra} and \code{exactextractr} (both in
\code{Suggests}). Install with \code{install.packages(c("terra", "exactextractr"))}.
}

\examples{
\dontrun{
library(geobr)
mt_mun <- read_municipality(code_muni = "MT", year = 2020)

# Monthly rainfall for Mato Grosso, 2020
chirps <- sus_grid_chirps(
  resolution    = "monthly",
  years         = 2020,
  municipalities = mt_mun,
  lang          = "pt"
)
sus_meta(chirps, "stage")  # "climate"
sus_meta(chirps, "type")   # "chirps"

# Daily rainfall, fire season
chirps_daily <- sus_grid_chirps(
  resolution    = "daily",
  years         = 2020,
  months        = 7:9,
  municipalities = mt_mun,
  agg_fun       = "mean",
  lang          = "en"
)

# Annual totals for all Brazil (no spatial aggregation)
paths <- sus_grid_chirps(
  resolution = "annual",
  years      = 2018:2022
)
}

}
\seealso{
\code{\link[=sus_grid_era5]{sus_grid_era5()}}, \code{\link[=sus_climate_anomaly]{sus_climate_anomaly()}}, \code{\link[=sus_mod_dlnm]{sus_mod_dlnm()}}
}
