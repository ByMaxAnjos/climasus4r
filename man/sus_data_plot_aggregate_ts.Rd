% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_data_plot_aggregate_ts.R
\name{sus_data_plot_aggregate_ts}
\alias{sus_data_plot_aggregate_ts}
\title{Time-Series Plot of Aggregated DATASUS Health Outcomes}
\usage{
sus_data_plot_aggregate_ts(
  df,
  value_col = NULL,
  group_col = NULL,
  facet_col = NULL,
  facet_ncol = 3L,
  plot_type = c("epidemic", "seasonal", "heatmap", "trend"),
  smooth_method = "loess",
  smooth_span = 0.25,
  log_transform = FALSE,
  free_scales = TRUE,
  palette = "lancet",
  title = NULL,
  date_labels = "\%b/\%y",
  year_breaks = "3 months",
  base_size = 11,
  interactive = FALSE,
  lang = "pt",
  verbose = TRUE
)
}
\arguments{
\item{df}{A \code{climasus_df} at stage \code{"aggregate"} or later (output of
\code{\link[=sus_data_aggregate]{sus_data_aggregate()}}).}

\item{value_col}{Character. Name of the outcome column to plot. If \code{NULL}
(default), the function auto-detects the first matching column from:
\code{n_obitos}, \code{n_internacoes}, \code{n_nascimentos}, \code{n_casos},
\code{n_procedimentos}, \code{n_estabelecimentos}, and their English / Spanish
equivalents.}

\item{group_col}{Character. Optional column used for colour-grouping in
\code{"epidemic"} and \code{"seasonal"} plots.}

\item{facet_col}{Character. Optional column for \code{facet_wrap()} panels.}

\item{facet_ncol}{Integer. Number of facet columns. Default \code{3L}.}

\item{plot_type}{Character. One or more of \code{"epidemic"}, \code{"seasonal"},
\code{"heatmap"}, \code{"trend"}. When multiple types are supplied, plots are
assembled with \link[patchwork:patchwork-package]{patchwork::patchwork} (one per row). Default: \code{"epidemic"}.}

\item{smooth_method}{Character. Smoothing method for the \code{"epidemic"} plot:
\code{"loess"} (default), \code{"gam"}, or \code{"lm"}.}

\item{smooth_span}{Numeric. Span for LOESS smoothing. Default \code{0.25}.}

\item{log_transform}{Logical. Apply \code{log1p} transformation on the y-axis
for \code{"epidemic"} and \code{"seasonal"} plots, and to the fill scale in
\code{"heatmap"}. Default \code{FALSE}.}

\item{free_scales}{Logical. Allow free y-axis scales in faceted plots.
Default \code{TRUE}.}

\item{palette}{Character. Colour palette. One of \code{"lancet"} (default),
\code{"nejm"}, \code{"jco"}, \code{"uchicago"}, or \code{"viridis"}.}

\item{title}{Character. Plot title. Auto-generated if \code{NULL}.}

\item{date_labels}{Character. \code{strftime} format string for x-axis date
labels. Default \code{"\%b/\%y"}.}

\item{year_breaks}{Character. \code{date_breaks} string for the x-axis. Default
\code{"3 months"}.}

\item{base_size}{Numeric. Base font size for the ggplot2 theme. Default \code{11}.}

\item{interactive}{Logical. Return a \code{\link[plotly:ggplotly]{plotly::ggplotly()}} widget instead of
a static ggplot2 object. Default \code{FALSE}.}

\item{lang}{Character. Language for labels and messages: \code{"pt"} (default),
\code{"en"}, or \code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
Invisibly returns the ggplot2 object (or plotly widget when
\code{interactive = TRUE}). Side effect: prints the plot in the active graphics
device.
}
\description{
Produces publication-quality time-series visualisations (epidemic curve,
seasonal boxplot, calendar heatmap, or annual trend) from a \code{climasus_df}
dataset at stage \code{"aggregate"} or later. Colour palettes are powered by
\link[ggsci:ggsci-package]{ggsci::ggsci} (Lancet, NEJM, JCO, UChicago). An interactive \link[plotly:plotly]{plotly::plotly} widget can
be returned with \code{interactive = TRUE}.
}
\section{Supported systems}{

Automatically adapts y-axis labels and titles to SINAN, SIM, SIH, SINASC,
SIA, and CNES via outcome column auto-detection.
}

\examples{
\dontrun{
# Epidemic curve, Portuguese labels
sus_data_plot_aggregate_ts(df_agg, lang = "pt")

# Seasonal boxplot, log-transformed
sus_data_plot_aggregate_ts(
  df_agg,
  plot_type     = "seasonal",
  log_transform = TRUE,
  lang          = "en"
)

# Calendar heatmap by group
sus_data_plot_aggregate_ts(
  df_agg,
  plot_type  = "heatmap",
  facet_col  = "municipio",
  facet_ncol = 2L,
  lang       = "pt"
)

# Multi-panel: epidemic + trend
sus_data_plot_aggregate_ts(
  df_agg,
  plot_type = c("epidemic", "trend"),
  lang      = "en"
)

# Interactive plotly widget
sus_data_plot_aggregate_ts(df_agg, interactive = TRUE)
}

}
