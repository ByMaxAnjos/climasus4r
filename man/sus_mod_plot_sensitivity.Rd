% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sus_mod_plot_sensitivity.R
\name{sus_mod_plot_sensitivity}
\alias{sus_mod_plot_sensitivity}
\title{Plots and Tables from a Multi-Stratum Sensitivity Analysis}
\usage{
sus_mod_plot_sensitivity(
  x,
  type = c("curves", "scatter", "bar"),
  output_type = c("plot", "table", "all"),
  interactive = FALSE,
  base_size = 12L,
  save_plot = NULL,
  lang = c("pt", "en", "es"),
  verbose = FALSE
)
}
\arguments{
\item{x}{A \code{climasus_sensitivity} object produced by \code{\link[=sus_mod_sensitivity]{sus_mod_sensitivity()}}.}

\item{type}{Character. Plot type: \code{"curves"} (default), \code{"scatter"}, or
\code{"bar"}.}

\item{output_type}{Character. \code{"plot"} (default), \code{"table"}, or \code{"all"}
(named list \verb{$plot}, \verb{$table}, \verb{$data}).}

\item{interactive}{Logical. \code{TRUE} converts the ggplot2 output to an
interactive \pkg{plotly} widget. Default \code{FALSE}.}

\item{base_size}{Numeric. ggplot2 base font size. Default \code{12}.}

\item{save_plot}{Character or \code{NULL}. File path to save the plot.}

\item{lang}{Character. Language for labels: \code{"pt"} (default), \code{"en"}, \code{"es"}.}

\item{verbose}{Logical. Print progress messages. Default \code{FALSE}.}
}
\value{
Depending on \code{output_type}:
\itemize{
\item \code{"plot"} -> a \code{ggplot} or \code{plotly} object.
\item \code{"table"} -> a \code{tibble} of the plotted data.
\item \code{"all"} -> a named list: \verb{$plot}, \verb{$table}, \verb{$data}.
}
}
\description{
Produces exposure-response curve overlays, hot-vs-cold RR scatter plots,
and grouped forest plots from a \code{climasus_sensitivity} object returned by
\code{\link[=sus_mod_sensitivity]{sus_mod_sensitivity()}}.
}
\section{Plot types (\code{type})}{
\tabular{ll}{
   \code{type} \tab Description \cr
   \code{"curves"} \tab Overlay of each stratum's exposure-response curve with CI ribbon \cr
   \code{"scatter"} \tab Hot RR vs Cold RR per stratum; point size encodes sensitivity index \cr
   \code{"bar"} \tab Horizontal forest plot of hot and cold RR per stratum ordered by SI \cr
}
}

\examples{
\dontrun{
sens <- sus_mod_sensitivity(fits, lang = "pt")

sus_mod_plot_sensitivity(sens, type = "curves",  lang = "pt")
sus_mod_plot_sensitivity(sens, type = "scatter", lang = "en")
sus_mod_plot_sensitivity(sens, type = "bar",     lang = "es")
out <- sus_mod_plot_sensitivity(sens, output_type = "all")
out$table
}

}
\seealso{
\code{\link[=sus_mod_sensitivity]{sus_mod_sensitivity()}}, \code{\link[=sus_mod_plot_dlnm]{sus_mod_plot_dlnm()}}
}
